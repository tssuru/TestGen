\documentclass[10pt, ukrainian]{article}
\setlength\textwidth{190mm} \setlength\textheight{277mm}
\setlength\voffset{-38mm} \setlength\hoffset{-38mm}
\setlength\parindent{0mm}
\pagestyle{empty}
\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage[ukrainian]{babel}
\begin{document}
{\center Екзаменаційний білет \No \textbf { 1 }\par}
{1. Основні підходи до побудови рекомендаційних систем.\par
2. Розробіть MapReduce-алгоритм, що за двома множинами з повтореннями (кожна множина зображується в окремому файлі послідовністю своїх елементів) знаходить їх різницю (якщо елемент входить в першу множину $n$ раз, а в другу~--- $m$ раз, то в їх різницю він входить $max(n-m,0)$ раз).\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 2 }\par}
{1. Фільтрування потоків у режимі реального часу. Фільтр Блума та його властивості.\par
2. Розробіть MapReduce-алгоритм, що за двома множинами з повтореннями (кожна множина зображується в окремому файлі послідовністю своїх елементів) знаходить їх перетин (якщо елемент входить в першу множину $n$ раз, а в другу~--- $m$ раз, то в їх перетин він входить $min(n,m)$ раз).\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 3 }\par}
{1. Алгоритми ієрархічної кластеризації. Суть ієрархічної кластеризації, стратегії вибору кластерів для злиття, критерії останова, ефективність.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 20\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}для університету $U$ оцінити середню чисельність студентів, що вивчають його дисципліни.\par\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 4 }\par}
{1. Пошук подібностей у документах у випадку високого рівня збігу.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 15\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}оцінити відсоток курсів університету $U$, за якими не менше половини студентів отримали оцінку $n$.\par\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 5 }\par}
{1. Неевклідові метричні простори, що зустрічаються в задачах обробки даних.\par
2. Доведіть, що якщо подібність Жакара (Jaccard) двох множин дорівнює нулю, то за довільної хеш-функції minhash-значення цих множин є різними.\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 6 }\par}
{1. Типова схема алгоритму $K$-means та алгоритми реалізації її окремих кроків. Вибір значення $k$. Модифікація алгоритму $K$-means (алгоритм BFR) для нормально розподілених в евклідовому просторі даних.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 10\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}оцінити відсоток студентів, що мають середній бал $x$ та більше.\par\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 7 }\par}
{1. Алгоритми кластеризації потокових даних.\par
2. За допомогою $4k$ незалежних хеш-функцій для двох множин, що мають подібність Жакара (Jaccard) $s$, побудовано по 4 minhash-підписи довжини $k$. Яка ймовірність того, що хоча б одна пара підписів співпадає?\par
3. Задача від Пашка}
\newpage
{\center Екзаменаційний білет \No \textbf { 8 }\par}
{1. Специфіка обробки потокових даних. Побудова репрезентативної вибірки заданого об’єму та її специфіка для задач, де аналізуються частоти.\par
2. В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 9 }\par}
{1. MapReduce: загальна схема, приклади застосування та оцінки складності.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 5\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}оцінити відсоток курсів, за якими не менше половини студентів отримали оцінку $n$.\par\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 10 }\par}
{1. Алгоритми множення матриць та векторів за допомогою MapReduce.\par
2. Задано три множини: $\{1, 2, 3, 4\}$, $\{2, 3, 5, 7\}$, $\{2, 4, 6\}$. Обчислити усі попарні відстані Жакара (Jaccard) між ними.\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 11 }\par}
{1. Задача пошуку найчастіших підмножин та правил асоціації (association rules) у потоці множин. Її застосування до практичних задач.\par
2. В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Наскільки більше прийдеться  використати пам’яті, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 12 }\par}
{1. Алгоритм пошуку найчастіших підмножин за умови обмежень на кількість проходів по даним та у випадку потокових даних.\par
2. Розглянемо фільтр Блума. Нехай є можливість зберігати в пам’яті біт-масив довжини $n$. В якому випадку ймовір\-ність отримати ложно-позитивну відповідь фільтра більша: якщо використовувати $k$ різних хеш-функцій в один біт-масив довжини $n$ чи якщо $k$ різними хеш-функціями хешувати в $k$ різних біт-масивів довжини ${\frac{n}{k}}$? Відповідь обґрунтувати.\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 13 }\par}
{1. Апріорний алгоритм пошуку найчастіших підмножин та його модифікації.\par
2. Довести, що відстань Жакара (Jaccard) є метрикою.\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 14 }\par}
{1. Пошук подібностей у документах на основі дослідження $n$-грам з використанням підходів minhash та LSH.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 10\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}оцінити відсоток студентів університету $U$, що мають середній бал $x$ та більше.\par\par
3. Задача від Пашка}
\newpage
{\center Екзаменаційний білет \No \textbf { 15 }\par}
{1. Задача оцінювання кількості одиниць у двійковому потоці та алгоритми її розв’язання.\par
2. В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо взяти степені трійки?\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 16 }\par}
{1. Задача кластеризації. Основні дві групи алгоритмів її розв’язання. Обмеження, що впливають на вибір алгоритму кластеризації. Основні проблеми, що виникають в алгоритмах кластеризації.\par
2. Розглянемо фільтр Блума. Нехай є можливість зберігати в пам’яті біт-масив довжини $n$. В якому випадку ймовір\-ність отримати ложно-позитивну відповідь фільтра більша: якщо використовувати $k$ різних хеш-функцій в один біт-масив довжини $n$ чи якщо $k$ різними хеш-функціями хешувати в $k$ різних біт-масивів довжини ${\frac{n}{k}}$? Відповідь обґрунтувати.\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 17 }\par}
{1. Задача знаходження кількості різних елементів потоку та алгоритми її розв’язання.\par
2. Задано три множини: $\{1, 2, 3, 4\}$, $\{2, 3, 5, 7\}$, $\{2, 4, 6\}$. Обчислити усі попарні відстані Жакара (Jaccard) між ними.\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 18 }\par}
{1. Кластеризація в неевклідових просторах. Алгоритм GRGPF.\par
2. За допомогою $4k$ незалежних хеш-функцій для двох множин, що мають подібність Жакара (Jaccard) $s$, побудовано по 4 minhash-підписи довжини $k$. Яка ймовірність того, що хоча б одна пара підписів співпадає?\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 19 }\par}
{1. Задача оцінювання рівномірності розподілу даних у потоці та алгоритми її розв’язання.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 20\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}оцінити відсоток курсів університету $U$, за якими не менше половини студентів отримали оцінку $n$.\par\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 20 }\par}
{1. Алгоритми ієрархічної кластеризації. Суть ієрархічної кластеризації, стратегії вибору кластерів для злиття, критерії останова, ефективність.\par
2. Потік даних складається з четвірок\par
\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\
Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студен\-тів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par
\hspace{10pt} Припустимо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 5\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par
\hspace{20pt}для університету $U$ оцінити середню чисельність студентів, що вивчають його дисципліни.\par\par
3. Задача від Пашка}
{\center Екзаменаційний білет \No \textbf { 21 }\par}
{1. Апріорний алгоритм пошуку найчастіших підмножин та його модифікації.\par
2. В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?\par
3. Задача від Пашка}
\newpage
\end{document}
