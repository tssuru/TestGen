Розробіть MapReduce-алгоритм, що за двома множинами з повтореннями (кожна множина зображується в окремому файлі послідовністю своїх елементів) знаходить їх різницю (якщо елемент входить в першу множину $n$ раз, а в другу~--- $m$ раз, то в їх різницю він входить $max(n-m,0)$ раз).


Розробіть MapReduce-алгоритм, що за двома множинами з повтореннями (кожна множина зображується в окремому файлі послідовністю своїх елементів) знаходить їх перетин (якщо елемент входить в першу множину $n$ раз, а в другу~--- $m$ раз, то в їх перетин він входить $min(n,m)$ раз).


Довести, що відстань Жакара (Jaccard) є метрикою.


Задано три множини: $\{1, 2, 3, 4\}$, $\{2, 3, 5, 7\}$, $\{2, 4, 6\}$. Обчислити усі попарні відстані Жакара (Jaccard) між ними.


Доведіть, що якщо подібність Жакара (Jaccard) двох множин дорівнює нулю, то за довільної хеш-функції minhash-значення цих множин є різними.


За допомогою $4k$ незалежних хеш-функцій для двох множин, що мають подібність Жакара (Jaccard) $s$, побудовано по 4 minhash-підписи довжини $k$. Яка ймовірність того, що хоча б одна пара підписів співпадає? 


Розглянемо фільтр Блума. Нехай є можливість зберігати в пам’яті біт-масив довжини $n$. В якому випадку ймовірність отримати ложно-позитивну відповідь фільтра більша: якщо використовувати $k$ різних хеш-функцій в один біт-масив довжини $n$ чи якщо $k$ різними хеш-функціями хешувати в $k$ різних біт-масивів довжини ${\frac{n}{k}}$? Відповідь обґрунтувати.


Потік даних складається з четвірок\\{ENDL}\hspace{20pt} (університет, ID дисципліни, ID студента, оцінка за національною шкалою).\\{ENDL} Вважаємо, що назви всіх університетів є унікальними, всередині університету ідентифікатори (ID) дисциплін та студентів також є унікальними, але ідентифікатори дисциплін та студентів різних університетів можуть повторюватися. \par{ENDL}\hspace{10pt} При\-пус\-ти\-мо, що ми хочемо оперативно давати відповіді на запити стосовно даних у цілому, але можемо зберігати в пам’яті тільки 5\% існуючих даних. Як слід побудувати репрезентативну вибірку, якщо запитом є:\par \hspace{20pt}для університету $U$ оцінити середню
чисельність студентів, що вивчають його дисципліни.\par


{\center оцінити відсоток студентів, що мають середній бал $x$ та більше.}

{\center оцінити відсоток студентів університету $U$, що мають середній бал $x$ та більше.}

{\center оцінити відсоток курсів, за якими не менше половини студентів отримали оцінку $n$.}

{\center оцінити відсоток курсів університету $U$, за якими не менше половини студентів отримали оцінку $n$.}

В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq N$, $N$ фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо взяти степені трійки? 

В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq N$, $N$ фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?

В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq N$, $N$ фіксований параметр алгоритму), використовуються степені двійки. Наскільки більше прийдеться  використати пам’яті, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?
