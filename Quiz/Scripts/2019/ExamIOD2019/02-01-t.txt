Розробіть MapReduce-алгоритм, що за двома множинами з повтореннями (кожна множина зображується в окремому файлі послідовністю своїх елементів) знаходить їх різницю (якщо елемент входить в першу множину $n$ раз, а в другу~--- $m$ раз, то в їх різницю він входить $max(n-m,0)$ раз).


Розробіть MapReduce-алгоритм, що за двома множинами з повтореннями (кожна множина зображується в окремому файлі послідовністю своїх елементів) знаходить їх перетин (якщо елемент входить в першу множину $n$ раз, а в другу~--- $m$ раз, то в їх перетин він входить $min(n,m)$ раз).


Довести, що відстань Жакара (Jaccard) є метрикою.


Доведіть, що якщо подібність Жакара (Jaccard) двох множин дорівнює нулю, то за довільної хеш-функції minhash-значення цих множин є різними.


За допомогою $4k$ незалежних хеш-функцій для двох множин, що мають подібність Жакара (Jaccard) $s$, побудовано по 4 minhash-підписи довжини $k$. Яка ймовірність того, що хоча б одна пара підписів співпадає?  Відповідь обґрунтувати.


Розглянемо фільтр Блума. Нехай є можливість зберігати в пам’яті біт-масив довжини $n$. В якому випадку ймовір\-ність отримати ложно-позитивну відповідь фільтра більша: якщо використовувати $k$ різних хеш-функцій в один біт-масив довжини $n$ чи якщо $k$ різними хеш-функціями хешувати в $k$ різних біт-масивів довжини ${\frac{n}{k}}$? Відповідь обґрунтувати.

В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо взяти степені трійки?  Відповідь обґрунтувати.

В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Як зміниться точність алгоритму, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?  Відповідь обґрунтувати.

В алгоритмі DGIM оцінювання кількості одиниць двійкового потоку, що надійшли за останні $T$ моментів часу ($T\leq~N$, $N$~--- фіксований параметр алгоритму), використовуються степені двійки. Наскільки більше прийдеться  використати пам’яті, якщо дозволити мати не по одній-двом групам (basket) кожного розміру, а по дві-три?  Відповідь обґрунтувати.
