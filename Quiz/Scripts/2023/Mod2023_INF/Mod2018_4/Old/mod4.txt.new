const a1="Написати клас, серед членів якого є:{ENDL}"
const a2="У класі має бути означений мінімальний набір операцій, що дозволяють працювати з об’єктами класу; зокрема, встановлювати та отримувати значення членів-даних.{ENDL}"
const a3="Також для класу мають бути перевантажені:{SPACE}"
const a4="Означення класу та його методів має цілком відповідати принципам ООП. Означення методів мають бути записані за межами означення класу (за винятком =default та =delete). Оцінюватися, в тому числі, буде розстановка const та обробка помилок.{ENDL}"
const a5="Написати програму, в якій створюється масив з двох об’єктів класу та до елементів масиву застосовуються усі явно перевантажені операції. {ENDL}"

#a1#
ціле, статичний рядок типу string, незмінюване дійсне,{ENDL}
відкритий метод f, що якось змінює кожне змінюване поле об’єкту та повертає об’єкт після виконання перетворення,{ENDL}
відкритий метод g, що записує у файл #name#.txt два довільних рядки.{ENDL}

#a2#
#a3#
   оператор перетворення до типу double; оператор +; оператор <.
#a4#
#a5#

   
ціле, незмінюваний символ, статичне дійсне,{ENDL}відкритий метод f, що щось виводить та повертає об’єкт після виконання дій,{ENDL}відкритий метод g, що повертає другий рядок файлу #name#.txt.{ENDL}
оператор перетворення до типу int; оператор *; оператор !=.

дійсне, незмінюваний статичний рядок string, незмінюване ціле,{ENDL}відкритий метод f, що змінює кожне змінюване поле об’єкта та повертає об’єкт після виконання дій,{ENDL}відкритий метод g, що повертає третій рядок файлу #name#.txt.{ENDL}
оператор перетворення до типу string; оператор &&; оператор <.

символ, незмінюване довге ціле, статичне дійсне,{ENDL}відкритий метод f, що змінює кожне змінюване поле класу та повертає об’єкт після виконання дій,{ENDL}відкритий метод g, що записує у файл #name#.txt три довільних рядки.{ENDL}
оператор перетворення до типу long; оператор ||; оператор >.

дійсне, незмінюване статичне дійсне, незмінюване ціле,{ENDL}відкритий метод f, що виводить поля об’єкта та повертає об’єкт після виконання дій,{ENDL}відкритий метод g, що записує у файл #name#.txt два довільних рядки.{ENDL}
оператор перетворення до типу double; оператор /; оператор ?.

статичне ціле, незмінюване дійсне, ціле,{ENDL}відкритий метод f, що виводить поля класу та повертає об’єкт після виконання дій,{ENDL}відкритий метод g, що читає з вхідного потоку поточний рядок та повертає його.{ENDL}
оператор перетворення до типу char; оператор %; оператор ?.



   
