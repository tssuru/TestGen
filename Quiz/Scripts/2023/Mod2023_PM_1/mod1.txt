\documentclass[12pt, ukrainian]{article}
\setlength\textwidth{170mm} \setlength\hoffset{-25mm}
%\setlength\textheight{277mm} \setlength\voffset{-38mm}
%\setlength\parindent{0mm}
\pagestyle{empty}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[ukrainian]{babel}
\usepackage{graphicx}
\usepackage{caption}
\DeclareCaptionFormat{nocaption}{}
\captionsetup{format=nocaption,aboveskip=0pt,belowskip=0pt}
\usepackage[Export]{adjustbox} 
\adjustboxset{max size={0.9\linewidth}{0.9\paperheight}}
\begin{document}
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 1 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//4%8*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-0.5*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 7==True and 9!=3.0 or 4<=9.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True!=1)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True==4.0==2.0==6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<-4

print(f(-7) and f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-4

print(f(0) or f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(d):
    v=27
    if d: 
        v=5
    elif d>=-3:
         return 9
    else:
         v=8
    return v

print(g(-8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,3,1
def g(b):
    a+=3
    b=2
    c=5
    return a+b+c

a,b,c=6,8,0
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить вiдрiзку [2016; 2017]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 2 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/7/5*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**-2+True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 6.0>False or 7.0>=3 and 6<2)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8j">="1.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2!=3!=9!=6.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) or f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) and f(-9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(b):
    x=94
    if b!=0: 
        return 4
    if b>-5:
         x=2
    else:
         return 3
    return x

print(h(-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,9,5
def f(a):
    a=1
    b-=4
    c=1
    return a+b+c

a,b,c=4,5,1
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число бiльше за квадрат другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 3 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/3*4*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**2.0-False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8<5 and True!=5.0 or 6==8.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0>"False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7>8.0>False>5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=2

print(f(-2) or f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-2) and f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=95
    if a:
        return 2
    if b==-1:
         c=1
    else: 
        c=9
    return c

print(f(2,-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,2,4
def h(b):
    a=3
    b*=2
    c=5
    return a+b+c

a,b,c=8,7,3
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї малої латинської літери. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 4 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//8//9*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2>=7 or not 4.0<=4 and 2.0>False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0<True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4 is 8 is 5.0 is False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-1) and f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=1

print(f(5) or f(8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=97
    if a>=-5:
        return 5
    elif b<=0:
         return 4
    else: 
        c=3
    return c

print(h(0,4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,9,9
def g(a):
    a=4
    b*=4
    c=1
    return a+b+c

a,b,c=0,6,5
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2017; 2018). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 5 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/9//6*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**-4--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0<3 or not 9!=5 or 6.0>False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("6"=="2.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9<9.0<8<3.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-1

print(f(7) and f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=4

print(f(-6) or f(4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(c):
    u=78
    if c==5: 
        u=1
    elif c<-4:
         return 0
    else:
         return 6
    return u

print(f(0))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,4,9
def f(a):
    a=3
    b=4
    c=5
    return a+b+c

a,b,c=0,7,6
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 6 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//5/7*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**1.0*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8==3 and 5.0<=7 and True>=7.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5<="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0<=True<=5<=7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) or f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) and f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=27
    if a<-2:
        c=8
    if b!=5:
         return 0
    else: 
        c=7
    return c

print(g(-5,1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,2,8
def h(a):
    a+=5
    b=3
    c=2
    return a+b+c

a,b,c=1,3,7
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є вісімковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 7 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/6%3*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**1.0+-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2>=True and 6!=8.0 and 4==4.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False">"4.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3>=2.0>=6>=2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-9) or f(-5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-2

print(f(1) or f(-5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a):
    y=23
    if a: 
        y=7
    if a<=4:
         return 5
    else:
         y=0
    return y

print(f(8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,1,5
def g(b):
    a=1
    b=2
    c=1
    return a+b+c

a,b,c=3,4,3
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх трійковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 8 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//4*6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**0.5*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8<=9.0 or 2.0<False or 5>9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<"3")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True==5.0==8.0==4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-3

print(f(9) and f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) and f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(b):
    w=14
    if b>2: 
        return 7
    if b<=-2:
         w=3
    else:
         return 9
    return w

print(h(3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,0,2
def h(a):
    a=2
    b=3
    c=4
    return a+b+c

a,b,c=9,7,1
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2018; 2019). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 9 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/7*9*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-0.5*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4.0>=2 and not 3.0==5 and True<6)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0>=2)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False!=9.0!=5!=9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-2

print(f(-3) or f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<-1

print(f(-4) and f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=61
    if a>3:
        return 6
    elif a>-4:
         return 4
    else: 
        c=0
    return c

print(g(8,2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,8,2
def f(b):
    a=4
    b-=1
    c=5
    return a+b+c

a,b,c=7,1,3
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дванадцятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 10 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//8/4*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**1.0+2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0<=9.0 or not False!=7 or 8>4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0<=False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6<3.0<2<3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(6) and f(-5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(3) or f(-5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(c):
    z=25
    if c>=3: 
        z=4
    elif c<-1:
         z=1
    else:
         return 8
    return z

print(g(5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,6,1
def h(b):
    a=5
    b=1
    c=5
    return a+b+c

a,b,c=5,8,7
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2019; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 11 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/9//7*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**0.5*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 9<6.0 and 3>=5.0 and True!=2)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"!="7j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False is 6.0 is True is 8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>0

print(f(6) or f(-2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-2) or f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=21
    if b:
        return 7
    if b<=4:
         c=1
    else: 
        c=8
    return c

print(h(-8,7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,0,9
def g(a):
    a=3
    b+=2
    c=3
    return a+b+c

a,b,c=6,2,4
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2017; 2018]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 12 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//5%8*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**1.0*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7==8 or not 6<=7.0 or False>2.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9=="8.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0<=7<=4<=4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) and f(-1))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=0

print(f(0) and f(1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=40
    if b<2:
        return 3
    elif a>=-3:
         return 6
    else: 
        c=9
    return c

print(f(-3,-7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,1,9
def h(b):
    a=5
    b*=1
    c=2
    return a+b+c

a,b,c=0,5,7
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 13 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//3%5*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**-1--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 7.0>=3 and 9.0<5 or True<=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True>"False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0>=False>=9>=2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=3

print(f(-7) or f(9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-6) or f(3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a):
    y=15
    if a==1: 
        return 6
    if a!=3:
         y=2
    else:
         return 0
    return y

print(h(-2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,6,5
def f(a):
    a-=4
    b=1
    c=5
    return a+b+c

a,b,c=4,0,6
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них не нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 14 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/6*3*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**-2-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4!=4.0 or 5>5.0 and 6==False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7.0j"==2.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0>7>True>3.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-6) and f(-9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-3

print(f(-7) and f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=46
    if a==1:
        return 5
    if a!=-3:
         c=2
    else: 
        c=2
    return c

print(f(1,-2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,8,9
def f(b):
    a+=2
    b=4
    c=3
    return a+b+c

a,b,c=7,1,3
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа і повертає логічну ознаку того, що вони попарно рiзнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 15 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//3/4*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-1.0*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True>=2 or 9<=8.0 or 3<3.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("4"!=1)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5>=7.0>=6>=4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-3) or f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==3

print(f(8) or f(4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(d):
    z=17
    if d<=4: 
        z=8
    elif d!=-1:
         z=3
    else:
         return 7
    return z

print(f(9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,9,2
def g(b):
    a=2
    b=3
    c=4
    return a+b+c

a,b,c=4,0,6
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа i повертає логічну ознаку того, що вони попарно рiвнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 16 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9/7//6*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**True+False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4!=False and 6.0==7 and 2.0>8)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8<=8.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0<=8<=3<=8.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==1

print(f(-8) and f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) and f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(d):
    w=46
    if d: 
        w=9
    elif d>=-4:
         return 4
    else:
         w=2
    return w

print(g(-3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,3,1
def g(b):
    a*=5
    b=3
    c=4
    return a+b+c

a,b,c=0,4,5
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число бiльше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 17 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/4%3*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**True+True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True<5 or 8.0>=3 and 8==9.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"<"5j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<5<4<4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) and f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(5) or f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=67
    if b==1:
        return 7
    elif b!=-4:
         return 3
    else: 
        c=0
    return c

print(g(-7,-5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,6,7
def h(a):
    a-=2
    b=1
    c=1
    return a+b+c

a,b,c=9,1,9
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше бiльше. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 18 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//9*7*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9!=7 and not False<=6 or 3.0>7.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7.0">=True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False is 3 is 9.0 is 9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>4

print(f(2) or f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-9) and f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=17
    if a:
        c=5
    if b>=4:
         return 4
    else: 
        c=9
    return c

print(h(7,-1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,8,4
def f(b):
    a+=3
    b=4
    c=5
    return a+b+c

a,b,c=7,6,2
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є двійковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 19 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//5//5*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-2-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4<2.0 or 2==6.0 and 3<=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>="9.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8!=4.0!=False!=7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-4) and f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-4

print(f(7) and f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(c):
    v=11
    if c>2: 
        return 6
    if c==5:
         return 1
    else:
         v=5
    return v

print(h(-7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,3,2
def h(a):
    a-=2
    b=2
    c=4
    return a+b+c

a,b,c=3,4,7
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що їх сума дiлиться на 3. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 20 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/6/8*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2>6 and not 5>=5.0 or True!=4.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>5.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6==2.0==True==2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=4

print(f(0) or f(-3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) or f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=33
    if a:
        return 8
    elif a<=5:
         c=6
    else: 
        return 1
    return c

print(g(3,-3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,1,5
def g(a):
    a*=3
    b=1
    c=5
    return a+b+c

a,b,c=6,9,8
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що друге більше. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 21 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/8//9*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 3.0==False or 9!=7 and 8>=6.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7"<="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>9.0>3.0>6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) or f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    x=48
    if a: 
        return 2
    if a<-3:
         x=6
    else:
         return 4
    return x

print(g(4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,4,1
def f(a):
    a=5
    b=2
    c=4
    return a+b+c

a,b,c=5,8,3
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 22 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//7%4*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-3--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2.0<4 and True<=8.0 or 8>6)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"<"6.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3==True==7.0==5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(-2) or f(-7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-3

print(f(-2) and f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    u=12
    if b>=1: 
        u=8
    elif b!=0:
         return 5
    else:
         u=9
    return u

print(f(1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,1,4
def h(b):
    a+=5
    b=2
    c=1
    return a+b+c

a,b,c=5,2,6
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що вони рiвнi з точнiстю до 0.01. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 23 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/3*8*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**0.5*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 3<4 and 9.0!=5.0 or 2>=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("6j"!=True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8<=8.0<=6.0<=9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-3

print(f(1) or f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-1

print(f(-6) and f(1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    w=10
    if b: 
        return 1
    elif b<=-2:
         w=3
    else:
         return 0
    return w

print(f(-5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,3,7
def f(b):
    a-=4
    b=3
    c=5
    return a+b+c

a,b,c=8,3,8
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 24 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//9/6*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**4+False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4.0>9 or not 7<=7.0 and True==5)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0==9)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>2>5.0>7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-1) and f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(0) or f(4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=58
    if b>2:
        return 2
    elif b<0:
         c=1
    else: 
        return 3
    return c

print(f(-6,-8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,7,2
def g(a):
    a*=3
    b=1
    c=2
    return a+b+c

a,b,c=5,1,6
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2018; 2019]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 25 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/8//9*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**-4+2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4.0<=7 and 2.0<True and 2==5)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>=1.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6<3<True<8.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-2

print(f(7) and f(-9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) or f(-8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=49
    if a>=-2:
        c=9
    if b==3:
         return 0
    else: 
        c=6
    return c

print(h(-9,5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,7,2
def h(a):
    a=3
    b=1
    c=2
    return a+b+c

a,b,c=9,0,6
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх сімковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 26 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//5*7*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**0.5*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not False>=4 or 3!=9 or 9.0>3.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2=="3")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5>=7.0>=3.0>=2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-6) or f(2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<-2

print(f(-9) and f(5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=83
    if a<=-5:
        c=7
    if b<-1:
         return 4
    else: 
        return 8
    return c

print(f(-4,-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,1,8
def g(b):
    a=4
    b=5
    c=3
    return a+b+c

a,b,c=9,3,4
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що вони дають однакову остачу при дiленнi на 5. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 27 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//6%5*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-2--2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8>6 or not 9<5.0 or 6.0<=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("4.0j"<="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4!=8!=False!=2.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=0

print(f(-8) and f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(3) and f(8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    u=77
    if d==-5: 
        u=7
    if d<-5:
         u=6
    else:
         return 2
    return u

print(h(-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,7,6
def f(a):
    a=1
    b=5
    c=2
    return a+b+c

a,b,c=2,4,1
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число безпечно дiлити на друге. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 28 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9/4/3*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-0.5*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 7.0>=8 and 6==True and 8.0!=2)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True!="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0 is 9 is 5.0 is False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) or f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==3

print(f(-3) or f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=16
    if b:
        c=5
    elif a!=4:
         c=8
    else: 
        return 1
    return c

print(h(4,8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,4,3
def g(b):
    a=4
    b*=5
    c=1
    return a+b+c

a,b,c=1,9,6
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є десятковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 29 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/4/7*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0>True or not 3==3.0 and 4>=7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1j"<"4.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7==5==4.0==8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==3

print(f(-5) and f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=1

print(f(-5) or f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    z=45
    if a>3: 
        z=7
    elif a<1:
         return 4
    else:
         z=8
    return z

print(g(7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,7,2
def f(a):
    a=3
    b+=2
    c=4
    return a+b+c

a,b,c=0,4,5
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за квадрат другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 30 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//7%9*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**False+True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 5!=2.0 and 7.0<2 or 8<=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>3.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0>True>2>6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(5) or f(2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) and f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=12
    if a>2:
        return 2
    if b!=3:
         c=3
    else: 
        return 4
    return c

print(g(9,6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,7,1
def h(b):
    a=2
    b-=1
    c=5
    return a+b+c

a,b,c=5,4,2
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2016; 2017). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 31 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//8*6*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**2-False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0!=4 or not 7>=6 and 9.0<False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>"9j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3 is 9 is 4 is False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) and f(-9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>4

print(f(5) or f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(c):
    y=75
    if c==-3: 
        return 0
    if c>-2:
         return 9
    else:
         y=5
    return y

print(f(-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,0,6
def f(a):
    a+=3
    b=4
    c=3
    return a+b+c

a,b,c=8,7,0
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2019; 2020]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 32 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/9//5*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-1.0*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 3<=5 and 9>True or 4.0==8.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7<"True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0!=7.0!=7!=7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-4

print(f(3) or f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) and f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=55
    if a:
        c=7
    elif b>-1:
         c=5
    else: 
        return 9
    return c

print(h(6,-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,6,9
def h(b):
    a*=2
    b=4
    c=5
    return a+b+c

a,b,c=8,5,2
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 33 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//5/8*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-0.5*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2>7.0 and 4!=False and 9.0==9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0>="2.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0>=3.0>=True>=4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-5) or f(-7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) and f(-6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(b):
    v=22
    if b: 
        v=1
    elif b!=0:
         return 3
    else:
         return 8
    return v

print(g(2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,4,7
def g(a):
    a=1
    b-=4
    c=5
    return a+b+c

a,b,c=1,8,5
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 34 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/3//4*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**False-True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 6<=2.0 or 5>=7 or True<6.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8.0"!=True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4.0<2<9.0<5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-1

print(f(-1) and f(7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=0

print(f(3) or f(-9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(c):
    x=34
    if c>=4: 
        x=6
    if c<=-1:
         return 1
    else:
         x=4
    return x

print(h(6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,0,4
def f(b):
    a=1
    b-=3
    c=2
    return a+b+c

a,b,c=9,3,6
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дев'ятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 35 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/6%3*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**-1+1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8<=4.0 and not True>3 and 8.0!=5)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8"<=3)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<=9<=2.0<=6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(6) or f(1))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=2

print(f(7) or f(-5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=39
    if a==-5:
        return 0
    if b>=-2:
         c=6
    else: 
        c=3
    return c

print(g(-2,0))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,2,8
def g(a):
    a+=1
    b=3
    c=5
    return a+b+c

a,b,c=3,5,1
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї великої латинської літери. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 36 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//3*3*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**2.0-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0==7 or not False<3.0 or 4>=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0=="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8==5.0==3==False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<1

print(f(-2) and f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) and f(4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=68
    if b<=-4:
        return 2
    elif a<0:
         return 0
    else: 
        c=1
    return c

print(f(5,3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,6,4
def h(b):
    a*=2
    b=4
    c=3
    return a+b+c

a,b,c=7,9,5
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що їх сума дiлиться на 3. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 37 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//7%7*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>5.0 and not 8<6 or 4.0!=3)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2>9.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0!=9.0!=7!=4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-3) and f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(0) or f(-2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    y=58
    if a==-4: 
        y=7
    elif a>=2:
         return 5
    else:
         y=2
    return y

print(g(-1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,8,7
def f(a):
    a=1
    b*=5
    c=2
    return a+b+c

a,b,c=1,0,3
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число бiльше за квадрат другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 38 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/5//6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-2+True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0==2 or not 7>=8.0 and True<=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<="5")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3>=False>=True>=4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>2

print(f(-4) or f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(8) and f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=57
    if b<=-3:
        c=5
    elif b!=5:
         return 8
    else: 
        c=7
    return c

print(f(-1,9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,7,3
def g(b):
    a=1
    b=4
    c=3
    return a+b+c

a,b,c=5,0,9
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за квадрат другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 39 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//9/8*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3>=2 and not 7.0==False or 6.0<=4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1.0j"=="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5>6>2>3.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=3

print(f(-4) and f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) or f(-9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    w=91
    if d<5: 
        return 9
    if d<=-5:
         w=0
    else:
         return 3
    return w

print(h(5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,2,8
def h(b):
    a=4
    b+=1
    c=3
    return a+b+c

a,b,c=3,2,0
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 40 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/8*5*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**0.5*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8>5 or 2.0!=True and 9.0<6)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"!="7.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0 is 9 is 7.0 is True)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) or f(9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-1

print(f(3) and f(-6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=32
    if a:
        return 6
    if a<1:
         return 4
    else: 
        c=9
    return c

print(h(2,4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,5,1
def g(a):
    a=2
    b-=4
    c=5
    return a+b+c

a,b,c=9,6,4
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше бiльше. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 41 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/6//4*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 5.0<=True or 6.0>6 and 3<7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<6)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8<7<3<6.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-3

print(f(-2) and f(-7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(-2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=60
    if b==-1:
        return 4
    elif a>1:
         c=1
    else: 
        return 6
    return c

print(g(-1,-3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,0,1
def f(b):
    a=3
    b=1
    c=5
    return a+b+c

a,b,c=5,7,6
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що вони дають однакову остачу при дiленнi на 5. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 42 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5//4*9*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-1.0*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0==2 and not False!=8.0 or 8>=4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0>="9j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<=8<=8.0<=2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-1) or f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=4

print(f(7) or f(4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=73
    if b>=4:
        c=9
    if a<3:
         c=5
    else: 
        return 3
    return c

print(h(1,0))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,0,4
def h(a):
    a=4
    b-=2
    c=5
    return a+b+c

a,b,c=3,1,9
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є десятковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 43 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//5/5*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**4+-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<=5 and not 9==3.0 and 9.0>2)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("9.0j">7)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0==9==4==4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(0) and f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-3

print(f(6) or f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=82
    if b>2:
        c=7
    elif a==-4:
         return 2
    else: 
        c=0
    return c

print(f(-5,5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,7,8
def f(b):
    a=1
    b*=3
    c=1
    return a+b+c

a,b,c=6,4,0
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2019; 2020]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 44 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/9%3*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**-3-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True>=6 or not 4.0!=5 or 8<2.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("5j"==2.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6 is True is 8.0 is 5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-2

print(f(-9) or f(1))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) and f(0))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=37
    if a:
        return 8
    if b>=5:
         return 4
    else: 
        c=6
    return c

print(g(5,-2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,9,4
def h(a):
    a=2
    b=4
    c=2
    return a+b+c

a,b,c=3,7,5
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дев'ятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 45 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/8//4*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**0.5*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not False>9 and 3==9.0 and 4>=3.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True">=False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2!=7.0!=4!=False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==3

print(f(-1) and f(1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(c):
    v=44
    if c: 
        return 1
    if c!=-1:
         v=3
    else:
         return 5
    return v

print(f(-5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,2,8
def h(b):
    a=3
    b=5
    c=4
    return a+b+c

a,b,c=9,1,3
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є двійковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 46 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//7/7*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**-4+2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7<=4.0 or not 2.0!=2 or True<4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("3"<="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0<=9<=5.0<=8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-4

print(f(4) or f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-5) or f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=86
    if a!=-2:
        c=3
    elif b<=-5:
         return 9
    else: 
        return 2
    return c

print(f(-8,-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,3,8
def g(b):
    a=5
    b+=4
    c=2
    return a+b+c

a,b,c=9,5,2
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2017; 2018]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 47 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//3%6*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8.0<=5.0 and 9!=6 and 5==False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0<True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<3<6<7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) and f(-3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-2

print(f(5) and f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=74
    if b:
        c=5
    if a>-3:
         c=8
    else: 
        return 7
    return c

print(h(-9,6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,1,3
def h(a):
    a+=3
    b=2
    c=4
    return a+b+c

a,b,c=0,2,9
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є вісімковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 48 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/4*8*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**1.0-False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3>6.0 or not 8<7 or 7.0>=True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8!="6.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0>True>5>3.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<1

print(f(-5) or f(9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) or f(8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    u=96
    if b: 
        u=9
    elif b>-2:
         u=8
    else:
         return 7
    return u

print(f(-7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,5,6
def g(b):
    a=1
    b*=5
    c=3
    return a+b+c

a,b,c=8,1,4
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2016; 2017). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 49 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5//6//9*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**-4+True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0<=3 and not 5!=4 or 4.0==False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False">="1.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0>=3>=5>=8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) or f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    x=40
    if d<-3: 
        x=0
    if d!=2:
         return 2
    else:
         return 4
    return x

print(h(-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,2,7
def f(a):
    a=4
    b-=2
    c=1
    return a+b+c

a,b,c=0,8,9
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 50 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/3/3*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**0.5*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True<5.0 or 2.0>=8 and 6>9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True=="1")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<3.0<9<2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=0

print(f(-9) or f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<0

print(f(-5) and f(5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=43
    if a!=0:
        return 0
    if b==-2:
         c=1
    else: 
        c=7
    return c

print(g(0,3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,4,1
def f(a):
    a=5
    b-=3
    c=1
    return a+b+c

a,b,c=3,0,9
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 51 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//6%8*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**4-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0==False and not 7!=3.0 and 2>7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0>6)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False is 9.0 is 7 is 6.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>4

print(f(1) or f(-2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(4) and f(3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=84
    if b<=-5:
        return 9
    elif a<3:
         c=5
    else: 
        return 3
    return c

print(f(6,-5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,7,8
def g(b):
    a+=5
    b=4
    c=2
    return a+b+c

a,b,c=4,5,3
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число бiльше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 52 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/4*9*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**False-False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True<=8.0 or 7.0<3 or 5>=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("2j"!="5.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0>5.0>4>6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) and f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-4

print(f(6) or f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    z=56
    if a==3: 
        z=6
    elif a>=4:
         return 2
    else:
         z=1
    return z

print(g(1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,6,9
def h(b):
    a=3
    b*=1
    c=2
    return a+b+c

a,b,c=1,2,8
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2018; 2019). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 53 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//9%6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-2+-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 6==2 and 5.0>=False or 8!=8.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"<4)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4==False==7.0==6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-3) and f(-5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) or f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(d):
    y=88
    if d: 
        return 9
    elif d<=1:
         y=3
    else:
         y=0
    return y

print(f(-8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,0,7
def f(a):
    a=4
    b*=5
    c=3
    return a+b+c

a,b,c=6,4,5
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 54 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/5//5*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-1.0*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4<=3 or not 6>7.0 and 9.0<True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<=4.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<=3<=5<=4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-1

print(f(4) or f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>1

print(f(-6) and f(1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=71
    if a:
        c=2
    elif b>=2:
         return 6
    else: 
        return 8
    return c

print(h(9,7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,7,0
def g(a):
    a+=1
    b=3
    c=5
    return a+b+c

a,b,c=9,8,3
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа i повертає логічну ознаку того, що вони попарно рiвнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 55 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//8*4*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2>7 or True>=5 and 4.0<=3.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1j"!="6.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True!=8!=8.0!=9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) or f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-2

print(f(0) or f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(b):
    u=60
    if b>-4: 
        return 8
    if b!=5:
         return 6
    else:
         u=7
    return u

print(g(0))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,6,2
def h(b):
    a-=4
    b=2
    c=4
    return a+b+c

a,b,c=4,8,2
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2019; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 56 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/7/7*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 9!=8 and False==6.0 or 2.0<4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<1.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2>=9.0>=False>=7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=0

print(f(7) and f(-1))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-2) and f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(c):
    x=99
    if c>=0: 
        return 5
    elif c<3:
         x=4
    else:
         return 6
    return x

print(h(-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,4,0
def g(b):
    a-=5
    b=3
    c=2
    return a+b+c

a,b,c=7,1,3
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що друге більше. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 57 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/4*3*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**2--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 6<7 and 5.0<=True and 8!=4.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False">6)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4!=2!=5.0!=7.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(3) or f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=1

print(f(-9) or f(7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a):
    w=66
    if a: 
        w=9
    if a==0:
         return 8
    else:
         w=5
    return w

print(h(4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,4,2
def g(a):
    a=1
    b=4
    c=1
    return a+b+c

a,b,c=9,3,1
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх сімковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 58 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//8/4*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0>=2 or not 8.0>9 or False==3)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"<="8.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0<=6.0<=False<=3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<3

print(f(-1) and f(-9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) and f(3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=79
    if b==-4:
        c=0
    if a<5:
         return 1
    else: 
        c=4
    return c

print(g(7,-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,6,2
def f(a):
    a+=1
    b=1
    c=3
    return a+b+c

a,b,c=3,8,4
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2017; 2018). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 59 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/9%6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not False>4 and 5>=9.0 and 6==7.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3==True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7>6>True>4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) and f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==0

print(f(4) and f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(d):
    v=39
    if d<=5: 
        return 4
    if d>-1:
         v=2
    else:
         return 0
    return v

print(g(6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,7,0
def f(a):
    a=3
    b=2
    c=5
    return a+b+c

a,b,c=4,8,5
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що вони рiвнi з точнiстю до 0.01. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 60 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5//5//9*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8<=3 or not True<6.0 or 2.0!=4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0>="4")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8 is 3.0 is 9 is 5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=2

print(f(-3) or f(9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-3) or f(-8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    z=20
    if b!=2: 
        z=7
    elif b>-4:
         z=1
    else:
         return 3
    return z

print(f(7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,9,7
def h(b):
    a*=5
    b=4
    c=2
    return a+b+c

a,b,c=0,6,5
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них не нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 61 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/6%7*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**0.5*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 9==8.0 and 5>False and 9.0!=7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0<"4.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<2.0<9<2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(6) and f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-1

print(f(7) or f(5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(c):
    v=33
    if c: 
        v=2
    elif c>=-5:
         return 1
    else:
         return 3
    return v

print(g(-3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,2,8
def f(b):
    a=5
    b=2
    c=3
    return a+b+c

a,b,c=7,5,6
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дванадцятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 62 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//3*8*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**2.0+False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2<=3 or not 5.0<8 or 3.0>=True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"==False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6>=3.0>=False>=5.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<-3

print(f(-6) or f(-5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(6) and f(-6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a):
    w=68
    if a==1: 
        w=4
    if a<-2:
         w=9
    else:
         return 7
    return w

print(f(-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,0,4
def h(a):
    a=4
    b=1
    c=5
    return a+b+c

a,b,c=9,5,3
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить вiдрiзку [2016; 2017]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 63 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/7//5*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**-3-True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True==5 or 2.0>9 or 6>=6.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7>="5")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4==5==3==6.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=4

print(f(0) and f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    y=74
    if d<=4: 
        return 8
    elif d<-3:
         y=6
    else:
         y=5
    return y

print(h(-1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,3,0
def h(a):
    a=5
    b*=3
    c=1
    return a+b+c

a,b,c=9,1,7
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї великої латинської літери. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 64 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//3/8*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**-2+-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4!=2 and 7<4.0 and False<=7.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8j">7.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0>=True>=4.0>=8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=4

print(f(-8) or f(7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) or f(-9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a):
    x=52
    if a>=4: 
        return 0
    if a>-3:
         return 7
    else:
         x=1
    return x

print(h(9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,2,5
def g(b):
    a+=4
    b=2
    c=2
    return a+b+c

a,b,c=4,1,9
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа і повертає логічну ознаку того, що вони попарно рiзнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 65 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//8%9*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 5!=8 and 9.0==False or 4.0>=3)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("3.0j"<="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7!=7!=False!=9.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) or f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-4) and f(-5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(c):
    z=76
    if c==-1: 
        z=8
    if c<=3:
         return 2
    else:
         return 5
    return z

print(f(-2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,2,8
def f(a):
    a-=1
    b=5
    c=3
    return a+b+c

a,b,c=4,6,3
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2018; 2019]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 66 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/7*6*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**-1-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4>7.0 or 3.0<2 and True<=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False!=9)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0==9==2==4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=1

print(f(1) and f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(-2) or f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=65
    if b!=4:
        c=8
    elif b<=-1:
         return 7
    else: 
        return 0
    return c

print(f(8,8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,0,2
def f(b):
    a=4
    b*=4
    c=3
    return a+b+c

a,b,c=7,6,1
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї малої латинської літери. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 67 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//5//3*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**False+-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0==7 or not 6<=9 and 8.0>False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0!="3.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6<True<7.0<5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-4

print(f(-2) or f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-4

print(f(9) or f(-2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=18
    if a>-3:
        c=6
    if a>=1:
         c=9
    else: 
        return 5
    return c

print(g(3,-1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,4,9
def g(a):
    a=5
    b+=2
    c=1
    return a+b+c

a,b,c=0,3,8
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 68 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/6/7*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 7>=True and 5!=2.0 or 6.0<6)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("2j"=="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0 is 8 is 3 is 3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-4) and f(-3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-3) and f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(b):
    u=21
    if b: 
        u=6
    elif b!=-4:
         u=3
    else:
         return 9
    return u

print(g(8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,5,0
def h(a):
    a=2
    b-=3
    c=4
    return a+b+c

a,b,c=3,2,6
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 69 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//4//5*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-2-True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3>5.0 or not 8>=6.0 or True<=4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8>False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0>6>3.0>False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-1) and f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=59
    if b:
        c=3
    elif a>0:
         return 4
    else: 
        c=2
    return c

print(h(-3,9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,8,9
def g(b):
    a=3
    b=4
    c=1
    return a+b+c

a,b,c=0,6,4
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число безпечно дiлити на друге. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 70 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/9/4*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2==9.0 and 3!=False and 6<4.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5<"2")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2<=True<=9<=5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-1

print(f(9) or f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-3

print(f(3) or f(-8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(b):
    x=13
    if b: 
        x=4
    if b!=0:
         return 0
    else:
         return 6
    return x

print(h(3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,8,1
def f(b):
    a+=1
    b=5
    c=3
    return a+b+c

a,b,c=7,0,1
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх трійковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 101 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/5%4*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**True+-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9!=8.0 and not False>7 and 4<=3.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0>="7.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0>6.0>4>False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-2

print(f(0) and f(7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(0) and f(-6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a):
    v=19
    if a==-2: 
        v=1
    elif a>=-5:
         return 4
    else:
         v=5
    return v

print(f(2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,3,6
def g(b):
    a=4
    b*=1
    c=2
    return a+b+c

a,b,c=4,7,2
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що вони рiвнi з точнiстю до 0.01. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 102 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//8*3*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**0.5*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8>=2 or not True==2.0 or 7.0<5)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"<=True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0==4.0==7==8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) or f(-2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>3

print(f(-5) or f(4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(c):
    w=35
    if c>5: 
        return 7
    elif c<1:
         w=8
    else:
         w=0
    return w

print(g(-3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,5,3
def h(a):
    a=5
    b-=5
    c=1
    return a+b+c

a,b,c=0,6,1
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2019; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 103 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/3*6*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**0.5*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0==7 or not 5<True or 9!=7.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7j">=4)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0!=2.0!=6!=True)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=4

print(f(-9) or f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=1

print(f(-1) and f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=19
    if b:
        return 1
    if a!=3:
         c=1
    else: 
        return 5
    return c

print(g(4,2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,9,4
def f(b):
    a*=2
    b=3
    c=4
    return a+b+c

a,b,c=7,5,2
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї малої латинської літери. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 104 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//9%8*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-3-True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4>=3 and False<=8.0 and 8>6.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8.0"!="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9<7<8<4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(6) and f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) or f(-9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=93
    if a>=5:
        return 4
    if a<-3:
         c=8
    else: 
        return 2
    return c

print(h(-2,-8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,4,8
def g(a):
    a+=1
    b=5
    c=3
    return a+b+c

a,b,c=5,2,0
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 105 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//6/7*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2<=6 and False<4.0 and 3.0>9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0<True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5 is 4 is 9.0 is False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) and f(2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(5) and f(7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=69
    if b==0:
        c=0
    elif b<=-1:
         c=6
    else: 
        return 7
    return c

print(f(-6,1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,1,6
def h(a):
    a-=4
    b=2
    c=3
    return a+b+c

a,b,c=9,6,0
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число безпечно дiлити на друге. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 106 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/7//5*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**True+-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5>=6 or not 5.0==2 or True!=9.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("6"==True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0>=2>=True>=3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==3

print(f(-5) or f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=2

print(f(1) or f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=99
    if a>2:
        return 3
    elif a<-4:
         c=9
    else: 
        c=7
    return c

print(f(-7,-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,2,9
def f(b):
    a-=4
    b=2
    c=5
    return a+b+c

a,b,c=8,1,3
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 107 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/4%9*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0<=4.0 or not 3>=7 and False==4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1.0j">4.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0<=8<=False<=2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) or f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-3

print(f(-1) or f(8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=15
    if a:
        return 0
    if b>=-5:
         return 9
    else: 
        c=2
    return c

print(h(-4,-7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,1,4
def f(b):
    a=2
    b=5
    c=3
    return a+b+c

a,b,c=7,5,6
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число бiльше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 108 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5//7*5*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0!=8 and not 6>True or 4<5.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"<=3)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>=7.0>=5>=5.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=1

print(f(5) and f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) and f(1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=81
    if b!=4:
        return 4
    if b==-2:
         c=6
    else: 
        return 8
    return c

print(g(-1,3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,4,9
def g(b):
    a=1
    b+=5
    c=3
    return a+b+c

a,b,c=7,8,3
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що друге більше. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 109 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/4//4*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**4-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 9.0!=7.0 and 7<=3 or 5>False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("5.0j"<False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9!=9.0!=3.0!=3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-6) and f(-7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>3

print(f(2) and f(-8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    u=79
    if d<=2: 
        return 9
    if d<5:
         return 3
    else:
         u=2
    return u

print(h(2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,2,1
def g(a):
    a=1
    b=2
    c=4
    return a+b+c

a,b,c=8,9,0
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є десятковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 110 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//3/6*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**-4+1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 9<8.0 or 8>=2 and 3.0==True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0!="1j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7 is True is 6 is 2.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=0

print(f(-5) or f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-6) or f(-2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=44
    if a:
        c=3
    elif a<=1:
         c=1
    else: 
        return 5
    return c

print(h(7,-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,5,2
def h(a):
    a*=4
    b=2
    c=1
    return a+b+c

a,b,c=6,4,0
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дев'ятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 111 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//6%3*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**-2+-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6==6.0 or not 4.0>3 or 7>=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9<="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<=6<=8<=4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-1) or f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(4) and f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=34
    if b>=4:
        return 7
    if a==-1:
         c=5
    else: 
        return 9
    return c

print(g(3,-7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,5,1
def h(a):
    a=1
    b=5
    c=2
    return a+b+c

a,b,c=7,3,6
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число бiльше за квадрат другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 112 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/5//7*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**-2-True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True!=3.0 and 9.0<=8 and 2<4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7">2)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2<8.0<6.0<7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<-1

print(f(-3) and f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<-2

print(f(3) or f(0))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    y=36
    if b!=1: 
        return 1
    elif b==3:
         y=9
    else:
         y=0
    return y

print(f(7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,5,2
def h(b):
    a=1
    b*=4
    c=3
    return a+b+c

a,b,c=1,0,7
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2018; 2019). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 113 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//9/8*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**1.0--2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 5>=8.0 and 2.0<9 or 9!=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7.0">="True")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9>False>5.0>4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-2) or f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=4

print(f(-7) and f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=85
    if b<3:
        c=8
    elif b<=-3:
         c=6
    else: 
        return 4
    return c

print(f(1,-1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,9,6
def g(a):
    a=2
    b+=5
    c=4
    return a+b+c

a,b,c=3,5,6
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2019; 2020]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 114 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/8*9*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**0.5*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 5.0>7 or 7.0==6 and 2<=True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4.0==False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0==3==True==5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-2

print(f(1) and f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) or f(-5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    z=87
    if a: 
        return 5
    if a>=2:
         return 3
    else:
         z=4
    return z

print(g(4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,4,0
def f(a):
    a-=3
    b=1
    c=2
    return a+b+c

a,b,c=2,9,8
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 115 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5//5/9*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-0.5*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<=6.0 and not 2.0!=5 or 4>=3)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("5.0j"<="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7>=5>=7.0>=8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-4

print(f(-9) and f(7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-4

print(f(-9) or f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=94
    if b:
        c=2
    if a!=-4:
         return 1
    else: 
        return 3
    return c

print(h(-9,6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,3,5
def f(b):
    a=5
    b-=2
    c=5
    return a+b+c

a,b,c=7,2,0
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що їх сума дiлиться на 3. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 116 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/4%4*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**True+2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>8 or not 4.0<4 and 9==3.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0>8)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5.0==False==2==2.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) or f(-9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(5) and f(7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(d):
    z=97
    if d: 
        return 8
    if d>-5:
         z=7
    else:
         return 2
    return z

print(f(-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,4,3
def g(b):
    a=4
    b+=1
    c=3
    return a+b+c

a,b,c=8,9,6
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2016; 2017). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 117 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/8*5*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-1.0*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0<=8 and not 7.0>=False or 2!=6)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("3j"==True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True!=9!=6!=9.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) or f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-1

print(f(6) and f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(c):
    w=18
    if c<=-2: 
        w=6
    elif c>=-4:
         w=1
    else:
         return 0
    return w

print(g(9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,2,9
def h(b):
    a=4
    b=3
    c=3
    return a+b+c

a,b,c=4,7,2
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа і повертає логічну ознаку того, що вони попарно рiзнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 118 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//3//3*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True>5 or not 3<5.0 and 8.0==7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<6)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0 is True is 4.0 is 3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-3

print(f(-1) and f(2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-4) or f(5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=36
    if a>0:
        c=0
    elif b>=-5:
         return 9
    else: 
        c=6
    return c

print(f(-2,-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,1,0
def g(a):
    a=1
    b=2
    c=4
    return a+b+c

a,b,c=4,5,3
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за квадрат другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 119 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//9%7*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**1.0*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 6==4.0 and 2>=5.0 or False>7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"!="9")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>7>3.0>2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=2

print(f(-2) and f(-5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) or f(-2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=11
    if b!=5:
        c=8
    elif a<=2:
         return 0
    else: 
        return 7
    return c

print(g(-8,-3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,2,5
def h(a):
    a*=3
    b=4
    c=5
    return a+b+c

a,b,c=1,9,6
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 120 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/7//6*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**2.0+1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9!=6.0 or not 3.0<=5 and 8<True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0>="8.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6<5<8.0<False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) or f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<0

print(f(-3) and f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    u=89
    if d!=0: 
        u=5
    if d>4:
         return 7
    else:
         u=6
    return u

print(h(8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,9,0
def f(b):
    a=5
    b=1
    c=3
    return a+b+c

a,b,c=8,4,2
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше бiльше. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 121 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//6/8*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**0.5*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4==True or not 3<=2.0 and 8.0>=7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"==True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<=6.0<=8<=8.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(4) or f(0))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>3

print(f(8) or f(-6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    x=62
    if b==-3: 
        return 3
    elif b<-1:
         x=4
    else:
         return 9
    return x

print(f(-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,0,7
def h(a):
    a=1
    b*=2
    c=4
    return a+b+c

a,b,c=4,9,1
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що вони дають однакову остачу при дiленнi на 5. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 122 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/8*7*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-1.0*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>4 and not 9<2 or 7.0!=9.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1"<3.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9 is 4 is 7.0 is 3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(8) and f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-5) and f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    y=55
    if a: 
        y=2
    elif a<=-1:
         return 8
    else:
         return 2
    return y

print(g(-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,0,3
def f(b):
    a=2
    b-=3
    c=5
    return a+b+c

a,b,c=4,6,2
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 123 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/9//9*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-0.5*-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8>3 and not True!=8.0 or 6>=5.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("2.0"<=5)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0<7<5<False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-4) and f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-9) or f(3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(c):
    v=61
    if c<=5: 
        v=3
    if c>=-2:
         return 0
    else:
         v=6
    return v

print(h(5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,1,3
def f(b):
    a=5
    b=2
    c=4
    return a+b+c

a,b,c=6,7,5
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2018; 2019]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 124 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//6*5*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**2.0-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0==5 or not 3<False and 4.0<=4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>="4j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True==2.0==3.0==9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==1

print(f(7) or f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-1

print(f(4) and f(0))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(c):
    w=29
    if c==1: 
        return 5
    if c>0:
         w=4
    else:
         w=1
    return w

print(g(-8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,7,3
def g(a):
    a+=1
    b=3
    c=4
    return a+b+c

a,b,c=7,0,8
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх трійковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 125 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/4%4*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**-1-False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8==False and 2.0>=2 and 9.0<=7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1.0j"!=9.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6>=4>=8>=4.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) and f(-3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-2

print(f(1) or f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a):
    y=16
    if a: 
        return 9
    elif a<3:
         return 7
    else:
         y=8
    return y

print(f(3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,9,4
def h(b):
    a=5
    b+=2
    c=1
    return a+b+c

a,b,c=2,5,6
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа i повертає логічну ознаку того, що вони попарно рiвнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 126 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//5/3*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**-4+2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 6<3.0 or 6.0!=9 or 5>True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True">4)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2<=3<=False<=5.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-2

print(f(-1) or f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(4) and f(-9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=89
    if a:
        c=3
    if b<1:
         return 2
    else: 
        c=1
    return c

print(g(-4,9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,1,2
def g(b):
    a=5
    b-=3
    c=2
    return a+b+c

a,b,c=0,5,4
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2017; 2018). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 127 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/7%8*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**1.0*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 3<=5.0 and 8!=True and 6==8.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("7.0"!=False)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2.0!=8!=9!=3)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-3) or f(2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==0

print(f(3) or f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=47
    if b==-2:
        c=5
    if b>5:
         return 4
    else: 
        return 4
    return c

print(h(-3,7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,3,9
def f(a):
    a=1
    b*=4
    c=4
    return a+b+c

a,b,c=7,8,0
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 128 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//3/6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-0.5*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0>False or not 7<9 or 5>=7.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"<=9)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True>5.0>False>7.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>-3

print(f(5) and f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-7) and f(8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=66
    if a<1:
        c=8
    elif a==-3:
         return 3
    else: 
        c=2
    return c

print(f(0,-2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,5,9
def h(a):
    a=5
    b+=2
    c=3
    return a+b+c

a,b,c=7,6,1
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них не нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 129 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/7*5*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**0.5*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not False>4 or 2>=6.0 or 2.0<=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("6j">5.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0!=4!=2!=5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) or f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-4

print(f(5) and f(-6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=38
    if a!=-5:
        return 6
    elif b>=-4:
         c=7
    else: 
        return 0
    return c

print(h(5,-8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,3,9
def g(b):
    a=1
    b*=2
    c=3
    return a+b+c

a,b,c=5,7,8
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї великої латинської літери. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 130 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//3//3*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**True+-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 4.0!=3.0 and 6<5 and True==8)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True==8.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6 is 4.0 is 7 is False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=3

print(f(-6) and f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(7) or f(-2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=92
    if a:
        c=1
    if a>-2:
         c=5
    else: 
        return 9
    return c

print(g(-5,5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,1,6
def f(a):
    a=4
    b-=1
    c=5
    return a+b+c

a,b,c=3,0,4
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є вісімковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 131 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6//9%7*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**-1-1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0>=5.0 or not 2!=3 or False>4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("4.0j"<7)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6<6.0<True<2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-5) or f(-2))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=4

print(f(0) or f(-3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=52
    if b<=0:
        c=4
    elif b>3:
         return 6
    else: 
        return 8
    return c

print(f(4,8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=7,6,0
def h(b):
    a+=1
    b=3
    c=4
    return a+b+c

a,b,c=1,5,3
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є двійковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 132 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9/6*4*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-1.0*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7==3.0 and not 3<=6.0 and 4<True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8">="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0==3==8.0==8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-4

print(f(-7) and f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) and f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    z=85
    if d!=4: 
        z=1
    elif d>-5:
         return 3
    else:
         z=7
    return z

print(h(-1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,2,9
def f(a):
    a=5
    b-=2
    c=5
    return a+b+c

a,b,c=4,5,2
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2017; 2018]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 133 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//4//8*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2**1.0+2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 8.0<=True or 9<9.0 and 7!=6)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("True"!=2)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>3.0>9.0>7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==4

print(f(0) or f(-8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-5) or f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(b):
    v=53
    if b>=2: 
        return 4
    if b!=-4:
         return 9
    else:
         v=2
    return v

print(h(-2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,1,6
def g(b):
    a*=2
    b=4
    c=1
    return a+b+c

a,b,c=7,4,8
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить вiдрiзку [2016; 2017]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 134 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8/8/6*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-3**-4--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 2.0==4.0 and False>=8 or 2>5)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>="5")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<=9<=5<=9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-9) and f(9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-3

print(f(-1) and f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(b):
    u=50
    if b: 
        return 8
    elif b<-3:
         u=5
    else:
         u=6
    return u

print(f(-5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,9,7
def g(b):
    a=3
    b*=3
    c=5
    return a+b+c

a,b,c=5,3,1
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх сімковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 135 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//5/9*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-2-True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5!=8 and not 6.0==False and 3.0<4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1.0"<=3.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4>=4.0>=7.0>=False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) or f(-9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) or f(3))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=91
    if b<=2:
        c=0
    if b<-1:
         c=3
    else: 
        return 5
    return c

print(h(-7,-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,0,9
def f(a):
    a=4
    b-=1
    c=2
    return a+b+c

a,b,c=4,2,8
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дванадцятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 136 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/4%5*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**0.5*-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4.0>7.0 or not 9<=6 or 3>=True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True>2.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7>8.0>3>2)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<0

print(f(5) and f(8))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>1

print(f(-3) and f(-4))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    x=24
    if a==2: 
        return 0
    if a<=-4:
         x=7
    else:
         return 5
    return x

print(g(6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,8,9
def h(a):
    a=5
    b+=1
    c=4
    return a+b+c

a,b,c=2,3,4
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є десятковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 137 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3/7*9*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-0.5*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False<=7 or not 9.0>=2 or 8!=8.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"==1)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<2.0<False<5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-1

print(f(-5) and f(-1))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(-9) and f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(c):
    v=95
    if c: 
        return 4
    elif c>-1:
         v=9
    else:
         return 1
    return v

print(f(1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=1,6,0
def f(b):
    a-=2
    b=3
    c=1
    return a+b+c

a,b,c=7,0,2
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 138 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9//3//7*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**-1.0*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 3>2 and True==5.0 and 9<2.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("3j"<"6.0j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6>=8>=6.0>=5.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(2) or f(-3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-1) or f(0))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(d):
    w=65
    if d!=0: 
        w=6
    if d==1:
         w=0
    else:
         return 2
    return w

print(g(-7))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,8,6
def g(b):
    a=4
    b*=5
    c=2
    return a+b+c

a,b,c=5,7,3
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх трійковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 139 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4//6*8*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**-2+1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not True<5.0 and 5>=6 and 4!=9.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("False"!=4.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2!=6!=9.0!=5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) and f(-6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-8) or f(6))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=62
    if a:
        return 9
    elif a!=4:
         c=2
    else: 
        return 1
    return c

print(g(8,1))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,1,7
def h(a):
    a=3
    b+=2
    c=4
    return a+b+c

a,b,c=2,6,9
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що перше число менше за куб другого. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 140 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/5%6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-1**4--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not False>7 or 3.0==6 or 9<=4.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True>6)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8.0==True==2.0==False)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=-4

print(f(-2) or f(1))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=-3

print(f(8) and f(7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=42
    if b==0:
        c=7
    if a>=-5:
         return 3
    else: 
        c=9
    return c

print(f(2,0))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,0,9
def g(a):
    a=5
    b=1
    c=4
    return a+b+c

a,b,c=6,1,5
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 3 числа i повертає логічну ознаку того, що вони попарно рiвнi. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 141 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7/9//4*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**1.0*False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4!=2.0 or not 8<=6.0 or False>2)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("1.0j"<="9j")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9<=3<=4.0<=8)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(6) and f(7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==-4

print(f(5) and f(-5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(d):
    u=70
    if d<=3: 
        u=3
    if d<-5:
         return 8
    else:
         return 0
    return u

print(h(0))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=8,2,7
def h(a):
    a=2
    b=3
    c=1
    return a+b+c

a,b,c=4,9,6
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2021; 2022]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 142 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//8/3*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**4+-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 7.0<8.0 and 5>=3 and True==7)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True=="3.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6.0 is 7 is True is 4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n==1

print(f(0) or f(-4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-2) or f(9))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=70
    if a>-4:
        return 0
    if a>=3:
         c=1
    else: 
        return 7
    return c

print(f(9,4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=2,8,0
def g(b):
    a=2
    b=5
    c=4
    return a+b+c

a,b,c=4,1,7
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2016; 2017). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 143 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9/7%3*9)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 5>3 and 8>=False and 2.0<6.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("8"<"False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5>=7.0>=3>=3.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(9) or f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-6) and f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=13
    if b:
        c=2
    elif b<2:
         c=8
    else: 
        return 4
    return c

print(g(6,-5))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,0,4
def h(a):
    a=1
    b*=3
    c=5
    return a+b+c

a,b,c=1,8,3
print(h(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх сімковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 144 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7//6//6*3)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(-2**False+True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2!=True or not 5.0==3.0 or 7<=9)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0>=4)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2 is False is 4 is 5.0)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=4

print(f(-7) and f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>4

print(f(4) or f(1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a):
    z=82
    if a: 
        z=8
    elif a>=-3:
         return 4
    else:
         z=3
    return z

print(g(8))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=6,9,7
def f(b):
    a-=5
    b=3
    c=2
    return a+b+c

a,b,c=4,1,8
print(f(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує рядок i повертає логічну ознаку того, що вiн складається рівно з однієї літери, яка є вісімковою цифрою. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 145 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6/4/5*4)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**-3-False)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<=7.0 and not 4>=6 or 9.0>5)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(True<="9.0")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9.0<=8<=9<=True)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<2

print(f(-8) or f(4))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>=-2

print(f(7) or f(5))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(c):
    x=73
    if c!=5: 
        return 7
    if c<4:
         x=2
    else:
         return 5
    return x

print(f(3))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,2,0
def g(b):
    a=4
    b+=1
    c=1
    return a+b+c

a,b,c=3,7,4
print(g(a),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що їх сума дiлиться на 3. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 146 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(8//3*9*6)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3**False--1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9==4.0 or not False!=8.0 and 6<4)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7>="False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(6<5.0<7.0<7)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(1) and f(5))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-4) and f(-8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=28
    if a==4:
        return 6
    if b<=-3:
         c=5
    else: 
        c=6
    return c

print(h(-6,2))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=5,1,8
def h(a):
    a=3
    b+=2
    c=5
    return a+b+c

a,b,c=3,2,6
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить iнтервалу (2018; 2019). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 147 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//8//8*7)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4**-0.5*True)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7!=8 or not 2<4.0 and True==2.0)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("2"==8.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3==False==8.0==4)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<=3

print(f(-4) and f(-7))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n!=2

print(f(-2) and f(-1))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(a,b):
    c=24
    if b!=-2:
        return 2
    elif b!=-1:
         return 5
    else: 
        c=8
    return c

print(h(4,9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=9,0,4
def g(a):
    a-=4
    b=2
    c=3
    return a+b+c

a,b,c=3,9,2
print(g(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 цілих числа i повертає логічну ознаку того, що їх дванадцятковий запис закінчується на одну ту саму цифру. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 148 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(5/9/4*8)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**-4+-2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(7.0>8.0 and not 3>=7 or 4<=False)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("2.0j">True)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0!=8!=7!=9)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-9) or f(6))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(3) or f(2))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def h(b):
    y=32
    if b==-2: 
        y=6
    elif b>=-3:
         y=1
    else:
         return 9
    return y

print(h(-4))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=3,5,7
def f(a):
    a=3
    b=4
    c=2
    return a+b+c

a,b,c=9,1,6
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно належить iнтервалу (2020; 2021). Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 149 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(4/5*7*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1**1.0*2)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(not 3<9 and 9.0>6 and 6.0!=True)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(1<"False")
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(False>2.0>6.0>5)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n>0

print(f(-3) or f(9))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n<3

print(f(-5) and f(-7))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def f(a,b):
    c=30
    if a:
        return 1
    elif a<=5:
         c=9
    else: 
        c=7
    return c

print(f(-3,-9))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=0,5,8
def f(b):
    a=1
    b*=4
    c=5
    return a+b+c

a,b,c=6,7,1
print(f(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує 2 числа і повертає логічну ознаку того, що хоча б одне з них не нуль. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
%begin
{\Large{16.10.2023 Контрольна робота \No 1, варіант  \No \textbf { 150 }\par}}
{
\begin {large}
%beginitem
1. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3//4%5*5)
%code

%enditem

%beginitem
2. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(9**0.5*1)
%code

%enditem

%beginitem
3. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(3.0==5 or not 8<=5.0 or False>=2)
%code
%enditem

%beginitem
4. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print("5j"!=6.0)
%code
%enditem

%beginitem
5. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 

%code
print(2 is 4.0 is True is 6)
%code
%enditem

%beginitem
6. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(0) and f(3))
%code
%enditem

%beginitem
7. Що буде виведено за виконання? (повідомлення інтерпретатора про помилку позначати error) 
%code

def f(n):
    print("f", end="");
    return n

print(f(-9) or f(8))
%code
%enditem

%beginitem
8. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
def g(a,b):
    c=20
    if b>=1:
        return 0
    if a==1:
         return 3
    else: 
        c=4
    return c

print(g(0,-6))
%code
%enditem

%beginitem
9. Що буде виведено за виконання (повідомлення інтерпретатора про помилку позначати error)?

%code
a,b,c=4,8,0
def h(b):
    a=5
    b=3
    c=1
    return a+b+c

a,b,c=5,3,2
print(h(b),a,b,c)
%code
%enditem

%beginitem
10. 
Написати функцiю f, що отримує число і повертає логічну ознаку того, що воно не належить вiдрiзку [2018; 2019]. Стиль запису умови також оцінюється. 


%enditem
\end {large}}
%end
%end
\end{document}

