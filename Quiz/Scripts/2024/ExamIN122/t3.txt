На прикладному рівні встановлюється з'єднання між процесами.

На прикладному рівні встановлюється з'єднання між мережевими інтерфейсами пристроїв.

У протоколі HTTP встановлюється з'єднання між мережевими інтерфейсами пристроїв.

У протоколі HTTP встановлюється з'єднання між комп'ютерами користувачів.

У протоколі HTTP встановлюється з'єднання між прикладними процесами.

Використання протоколів із встановленням з'єднання можливе тільки за фізичного дротового прямого з'єднання хостів.

Протокол TCP забезпечує надійну передачу даних.

Протокол IP забезпечує надійну передачу даних.

Протокол UDP забезпечує надійну передачу даних.

Протокол UDP може працювати як із встановленням з'єднання, так і без встановлення з'єднання.

Протокол UDP працює без встановлення з'єднання.

Протокол UDP працює із встановлення з'єднання.

Усі протоколи транспортного рівня вимагають встановлення з'єднання.

Протокол TCP має механізм контролю перенавантаження (congestion).

Протокол UDP має механізм контролю перенавантаження (congestion).

Протокол HTTP має механізм контролю перенавантаження (congestion).

Протокол SMTP має механізм контролю перенавантаження (congestion).

Протокол  HTTP є протоколом без збереження стану (stateless).

Протокол  HTTP є протоколом із збереженням стану (stateful).

Протокол  HTTP допускає постійне з'єднання.

З“єднання за протоколом  HTTP завжди є постійним.

З“єднання за протоколом  HTTP завжди є непостійним.

FTP є протоколом транспортного рівня.

BitTorrent – це протокол транспортного рівня.

Протокол TCP можна використовувати для широкомовлення.

Протокол TCP підтримує багатоадресну передачу даних.

Bluetooth є протоколом канального рівня.

Bluetooth є одним з протоколів стеку TCP/IP.

Естафетна передача виникає тоді, коли дані з однієї локальної мережі надходять в іншу.

Естафетна передача обробляє перехід телефона між сотами.

При передачі мультимедіа в потоковому режимі за втрати пакетів зазвичай виконується повторне відправлення втрачених пакетів.

При передачі файлів за протоколом TCP за втрати пакетів зазвичай виконується повторне відправлення втрачених пакетів.

При передачі файлів за протоколом UDP за втрати пакетів зазвичай виконується повторне відправлення втрачених пакетів.

При передачі файлів за втрати пакетів протокол IP виконує повторне відправлення втрачених пакетів.

