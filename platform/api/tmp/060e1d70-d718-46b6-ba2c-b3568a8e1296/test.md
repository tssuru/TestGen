# Контрольна робота

## Варіант 1

**1.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 'a'=3;
- ( 2 ) cout>>3;
- ( 3 ) bool r=('N'>'5');
- ( 4 ) x+=y;

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
9**-1.0*True
```

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
print(6 != 4 >= 6.0 == True)
```

**4.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) -3J
- ( 2 ) "a"a"
- ( 3 ) 0.5
- ( 4 ) 0b001

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=4,2,1 def h(a): global c    a=3 b*=4 c=5 return a+b+c a,b,c=5,6,0 print(h(b),a,b,c)

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (9 % 9 % 9 % 9);
```

**7.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

**8.** Відмітити все, що є коректним оператором С++:

- ( 1 ) (2+2j)//2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) x=y=z
- ( 4 ) x==y==z

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 9, 8, 7
def h(a, b=6, c=6):
    print(a, b, c, end=" ")

h(a=4, 2, c=1)
print(a, b, c)
```

**10.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') e=[10,11,12,13,14] e.extend([1,2]) print(e)

## Варіант 2

**1.** Що буде виведено за виконання фрагменту коду? a,b,c=4,2,1 def h(a): global c    a=3 b*=4 c=5 return a+b+c a,b,c=5,6,0 print(h(b),a,b,c)

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
e = ('a','b','c','d','e',0,1,2,3)
print(e[-8])
```

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 'a'=3;
- ( 2 ) cout>>3;
- ( 3 ) bool r=('N'>'5');
- ( 4 ) x+=y;

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (!6==4 || !6.0> true || 4.0<=3);
```

**5.** Що буде виведено за виконання фрагменту коду?

```cpp

a,b,c=9,8,7
def h(a,b=6,c=6):
    print(a,b,c,end="")

h(a=4,2,c=1)
print(a,b,c)

```

**6.** Що буде виведено за виконання фрагменту коду? x,y,z=4,2,1 y,z,y,z=9,z,y,x print(x,y,z)

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) (2+2j)//2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) x=y=z
- ( 4 ) x==y==z

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) x*y
- ( 2 ) (1)
- ( 3 ) False
- ( 4 ) For

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print(9**-1.0*True)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int h(int d){
    int y = 50;
    if (d == 0) 
        return 4;
    if (d > -2)
         y = 2;
    else
         y = 1;
    return y;
}

int main(){
    cout << h(-4);
    return 0;
}
```

## Варіант 3

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << 9 % 9 % 9 % 9;
```

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) -3J
- ( 2 ) "a"a"
- ( 3 ) 0.5
- ( 4 ) 0b001

**3.** Що буде виведено за виконання фрагменту коду? try: res = 9**-1.0*True if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**4.** Що буде виведено за виконання фрагменту коду? try: res = 8/8%8*8 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**5.** Вкажіть значення та тип виразу: "6.0"==True

**6.** Відмітити все, що є коректним оператором С++:

- ( 1 ) (2+2j)//2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) x=y=z
- ( 4 ) x==y==z

**7.** Що буде виведено за виконання фрагменту коду? try: for e in range(10, 10, -3): if e>=7: break print(e, end=';') if e>=7: break else: print(e,end=';') print(e) except: print('Z')

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int &x, int &y){
    x = 6;
    y+= 4;
    return x;
}

int main(){
    int a = 5, b = 9;
    a = f(a, a);
    cout << a << ":" << b <<':';
    {
        int a = 1, b = 8;
        cout << ((a>=5) && ((b+=1) >= 5))
             << a << ":" << b << ":";
    }
    {
        inta = 3;
        cout << a << ':';
    }
    cout << a << ":" << b << endl;
    return 0;
}
```

**9.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) x*y
- ( 2 ) (1)
- ( 3 ) False
- ( 4 ) For

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
m=4
while m<10:
    m+=1
print(m)
```

## Варіант 4

**1.** Що буде виведено за виконання фрагменту коду? a = 4 b = 2 c = 1 def h(a): global c    a = 3 b *= 4 c = 5 return a + b + c a = 5 b = 6 c = 0 try: print(h(b), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')

**2.** Записати ВИРАЗ, значенням якого є множина, що складається з цілих чисел від 15 до 2005 (включно), що не діляться на 7.

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) (2+2j)//2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) x=y=z
- ( 4 ) x==y==z

**4.** Записати ВИРАЗ, значенням якого є список, що складається з кубів цілих чисел від 15 до 2005 (включно), що не діляться на жодне з чисел 2, 3, записаних в оберненому порядку.

**5.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) x*y
- ( 2 ) (1)
- ( 3 ) False
- ( 4 ) For

**6.** Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення. Оцінюється правильність та якість коду.

**7.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) -3J
- ( 2 ) "a"a"
- ( 3 ) 0.5
- ( 4 ) 0b001

**8.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') for e in range(10,10,-3): if e>=7: break print(e, end=' ') if e>=7: break else: print(e, end=' ') print(e)

**9.** Що буде виведено за виконання фрагменту коду? e=[10,11,12,13,14]; e.extend([1,2]); print(e)

**10.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
