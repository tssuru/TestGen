# Контрольна робота

## Варіант 1

**1.** Відмітити все, що є коректним оператором С++:

- ( 1 ) double x=1;
- ( 2 ) cin<<3;
- ( 3 ) x=y=z
- ( 4 ) cin>>x+3;

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
4**-3--1
```

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
print(3.0 > 3 is 4 == False)
```

**4.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) true
- ( 2 ) 1+2j
- ( 3 ) xy
- ( 4 ) 1+1j

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=1,9,4 def h(b): global c    a*=3 b=1 c=5 return a+b+c a,b,c=5,7,3 print(h(a),a,b,c)

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (5 % 5 % 5 % 5);
```

**7.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в оберненому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

**8.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3**-1
- ( 2 ) if ('x'><'y'): a=6
- ( 3 ) x<y<z
- ( 4 ) (2+2j)%2

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 7, 8, 6
def h(a, b, c=9):
    print(a, b, c, end=" ")

h(1, 2, 4)
print(a, b, c)
```

**10.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') b=['0','1','2','3','4'] print(b.pop(-4), end=' ') print(b)

## Варіант 2

**1.** Що буде виведено за виконання фрагменту коду? a,b,c=1,9,4 def h(b): global c    a*=3 b=1 c=5 return a+b+c a,b,c=5,7,3 print(h(a),a,b,c)

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
b = ('a','b','c','d','e',0,1,2,3)
print(b[-11])
```

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) double x=1;
- ( 2 ) cin<<3;
- ( 3 ) x=y=z
- ( 4 ) cin>>x+3;

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (!3.0<=3 or !4> false or 6==4.0);
```

**5.** Що буде виведено за виконання фрагменту коду?

```cpp

a,b,c=7,8,6
def h(a,b,c=9):
    print(a,b,c,end="")

h(1,2,4)
print(a,b,c)

```

**6.** Що буде виведено за виконання фрагменту коду? x,y,z=1,2,4 y,z,y,z=7,z,x,y print(x,y,z)

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3**-1
- ( 2 ) if ('x'><'y'): a=6
- ( 3 ) x<y<z
- ( 4 ) (2+2j)%2

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) %
- ( 2 ) x+23
- ( 3 ) True
- ( 4 ) 2int

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print(4**-3--1)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int h(int b){
    int v = 62;
    if (b <= -3) 
        v = 1;
    else if (b > 3)
         return 9;
    else
         v = 4;
    return v;
}

int main(){
    cout << h(-7);
    return 0;
}
```

## Варіант 3

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << 5 % 5 % 5 % 5;
```

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) true
- ( 2 ) 1+2j
- ( 3 ) xy
- ( 4 ) 1+1j

**3.** Що буде виведено за виконання фрагменту коду? try: res = 4**-3--1 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**4.** Що буде виведено за виконання фрагменту коду? try: res = 5//5/5*5 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**5.** Вкажіть значення та тип виразу: 0<=False

**6.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3**-1
- ( 2 ) if ('x'><'y'): a=6
- ( 3 ) x<y<z
- ( 4 ) (2+2j)%2

**7.** Що буде виведено за виконання фрагменту коду? try: for b in range(14, 14, -3): if b<=4: break print(b, end=';') if b<=4: break else: print(b,end=';') print(b) except: print('Z')

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int &x, int &y){
    x = 3;
    y-= 9;
    return y;
}

int main(){
    int a = 5, b = 6;
    b = f(b, b);
    cout << a << ":" << b <<':';
    {
        int a = 4, b = 8;
        cout << ((b<=5) || ((a-=2) <= 5))
             << a << ":" << b << ":";
    }
    {
        inta = 1;
        cout << a << ':';
    }
    cout << a << ":" << b << endl;
    return 0;
}
```

**9.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) %
- ( 2 ) x+23
- ( 3 ) True
- ( 4 ) 2int

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
j=1
while j<=7:
    j+=2
print(j)
```

## Варіант 4

**1.** Що буде виведено за виконання фрагменту коду? a = 1 b = 9 c = 4 def h(b): global c    a *= 3 b = 1 c = 5 return a + b + c a = 5 b = 7 c = 3 try: print(h(a), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')

**2.** Записати ВИРАЗ, значенням якого є множина, що складається з цілих чисел від 12 до 2002 (включно), що діляться на 3.

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3**-1
- ( 2 ) if ('x'><'y'): a=6
- ( 3 ) x<y<z
- ( 4 ) (2+2j)%2

**4.** Записати ВИРАЗ, значенням якого є список, що складається з кубів цілих чисел від 12 до 2002 (включно), що не діляться на жодне з чисел 2, 3, записаних в оберненому порядку.

**5.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) %
- ( 2 ) x+23
- ( 3 ) True
- ( 4 ) 2int

**6.** Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення. Оцінюється правильність та якість коду.

**7.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) true
- ( 2 ) 1+2j
- ( 3 ) xy
- ( 4 ) 1+1j

**8.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') for b in range(14,14,-3): if b<=4: break print(b, end=' ') if b<=4: break else: print(b, end=' ') print(b)

**9.** Що буде виведено за виконання фрагменту коду? b=['0','1','2','3','4']; print(b.pop(-4),end=' '); print(b)

**10.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в оберненому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
