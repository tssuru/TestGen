# Контрольна робота

## Варіант 1

**1.** Відмітити все, що є коректним оператором С++:

- ( 1 ) cout<<'\n';
- ( 2 ) int n,n;
- ( 3 ) if ('x'><'y') a=6;
- ( 4 ) {a=5;}

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
-3**True+-2
```

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
print(2 == True <= 8 != 2.0)
```

**4.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) {
- ( 2 ) <+
- ( 3 ) =<
- ( 4 ) 0X2b

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=0,6,9 def h(a): global c    a=5 b*=2 c=1 return a+b+c a,b,c=7,8,3 print(h(a),a,b,c)

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (8 % 8 % 8 % 8);
```

**7.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

**8.** Відмітити все, що є коректним оператором С++:

- ( 1 ) F"1={x:5.2f};"
- ( 2 ) 'a'=3
- ( 3 ) True=1
- ( 4 ) x=float(4)

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 6, 9, 8
def h(a, b, c):
    print(a, b, c, end=" ")

h(0, 5)
print(a, b, c)
```

**10.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') a=[10,11,12,13,14] a.append('13') print(a)

## Варіант 2

**1.** Що буде виведено за виконання фрагменту коду? a,b,c=0,6,9 def h(a): global c    a=5 b*=2 c=1 return a+b+c a,b,c=7,8,3 print(h(a),a,b,c)

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
a = ('a','b','c','d','e',0,1,2,3,4)
print(a[4])
```

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) cout<<'\n';
- ( 2 ) int n,n;
- ( 3 ) if ('x'><'y') a=6;
- ( 4 ) {a=5;}

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (!2<true and !8!= 2.0 and 6==8.0);
```

**5.** Що буде виведено за виконання фрагменту коду?

```cpp

a,b,c=6,9,8
def h(a,b,c):
    print(a,b,c,end="")

h(0,5)
print(a,b,c)

```

**6.** Що буде виведено за виконання фрагменту коду? x,y,z=0,5,4 x,z,y,y=6,z,y,9 print(x,y,z)

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) F"1={x:5.2f};"
- ( 2 ) 'a'=3
- ( 3 ) True=1
- ( 4 ) x=float(4)

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) {
- ( 2 ) <+
- ( 3 ) <>
- ( 4 ) =

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print(-3**True+-2)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int h(int a){
    int u = 96;
    if (a < -1) 
        return 0;
    if (a != 2)
         u = 6;
    else
         return 9;
    return u;
}

int main(){
    cout << h(2);
    return 0;
}
```

## Варіант 3

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << 8 % 8 % 8 % 8;
```

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) {
- ( 2 ) <+
- ( 3 ) =<
- ( 4 ) 0X2b

**3.** Що буде виведено за виконання фрагменту коду? try: res = -3**True+-2 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**4.** Що буде виведено за виконання фрагменту коду? try: res = 7/7*7*7 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**5.** Вкажіть значення та тип виразу: 5<"True"

**6.** Відмітити все, що є коректним оператором С++:

- ( 1 ) F"1={x:5.2f};"
- ( 2 ) 'a'=3
- ( 3 ) True=1
- ( 4 ) x=float(4)

**7.** Що буде виведено за виконання фрагменту коду? try: for a in range(-4, -4, 3): if a<3: break print(a, end=';') if a<3: break else: print('end',end=';') print(a) except: print('Z')

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int &x, int &y){
    x = 5;
    y+= 8;
    return x;
}

int main(){
    int a = 6, b = 4;
    a = f(a, a);
    cout << a << ":" << b <<':';
    {
        int a = 7, b = 1;
        cout << ((a<7) && ((b+=1) < 7))
             << a << ":" << b << ":";
    }
    {
        inta = 9;
        cout << a << ':';
    }
    cout << a << ":" << b << endl;
    return 0;
}
```

**9.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) {
- ( 2 ) <+
- ( 3 ) <>
- ( 4 ) =

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
i=0
while i<9:
    i+=1
print(i)
```

## Варіант 4

**1.** Що буде виведено за виконання фрагменту коду? a = 0 b = 6 c = 9 def h(a): global c    a = 5 b *= 2 c = 1 return a + b + c a = 7 b = 8 c = 3 try: print(h(a), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')

**2.** Записати ВИРАЗ, значенням якого є множина, що складається з цілих чисел від 14 до 2011 (включно), що не діляться на 2.

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) F"1={x:5.2f};"
- ( 2 ) 'a'=3
- ( 3 ) True=1
- ( 4 ) x=float(4)

**4.** Записати ВИРАЗ, значенням якого є список, що складається з кубів цілих чисел від 14 до 2011 (включно), що не діляться на жодне з чисел 2, 3, записаних в оберненому порядку.

**5.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) {
- ( 2 ) <+
- ( 3 ) <>
- ( 4 ) =

**6.** Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення. Оцінюється правильність та якість коду.

**7.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) {
- ( 2 ) <+
- ( 3 ) =<
- ( 4 ) 0X2b

**8.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') for a in range(-4,-4,3): if a<3: break print(a, end=' ') if a<3: break else: print('end', end=' ') print(a)

**9.** Що буде виведено за виконання фрагменту коду? a=[10,11,12,13,14]; a.append('13'); print(a)

**10.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
