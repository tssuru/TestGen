# Контрольна робота

## Варіант 1

**1.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') b=['0','1','2','3','4'] print(b.index(1), end=' ')

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) x23
- ( 2 ) x+23
- ( 3 ) z13
- ( 4 ) ?qwerty

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 7, 8, 6
def g(a, b, c=9):
    print(a, b, c, end=" ")

g(1, 5, 0)
print(a, b, c)
```

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (4 / 4 / 4 / 4);
```

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=1,5,8 def g(b): global c    a-=2 b=3 c=5 return a+b+c a,b,c=3,4,9 print(g(a),a,b,c)

## Варіант 2

**1.** Що буде виведено за виконання фрагменту коду? x,y,z=1,5,0 y,z,x,x=y,7,z,x print(x,y,z)

**2.** Відмітити все, що є коректним оператором С++:

- ( 1 ) j=i;
- ( 2 ) cin<<x;
- ( 3 ) return 'a'<'c';
- ( 4 ) if ('x'<>'y') a=6;

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (!3.0<=false or !3!= 7.0 or 7<2);
```

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
b = ('a','b','c','d','e',0,1,2)
print(b[-12])
```

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=1,5,8 def g(b): global c    a-=2 b=3 c=5 return a+b+c a,b,c=3,4,9 print(g(a),a,b,c)
