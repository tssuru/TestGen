# Контрольна робота

## Варіант 1

**1.** Відмітити все, що є коректним оператором С++:

- ( 1 ) {a=5;}
- ( 2 ) char c='\n';
- ( 3 ) int f(int a,9);
- ( 4 ) if a<b a=0;

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
4**-0.5*True
```

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
print(6 != 2 > 4 < False)
```

**4.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) 0.5
- ( 2 ) or
- ( 3 ) -3J
- ( 4 ) "abc"""

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=4,0,2 def g(b): global c    a-=2 b=3 c=1 return a+b+c a,b,c=5,1,6 print(g(b),a,b,c)

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (9 / 9 / 9 / 9);
```

**7.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в оберненому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

**8.** Відмітити все, що є коректним оператором С++:

- ( 1 ) x=y=z
- ( 2 ) ++n
- ( 3 ) True=1
- ( 4 ) n=1; n++

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 7, 6, 8
def g(a, b, c=9):
    print(a, b, c, end=" ")

g(a=4, 0, b=2)
print(a, b, c)
```

**10.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') e=['0','1','2','3','4'] e.extend([1,2]) print(e)

## Варіант 2

**1.** Що буде виведено за виконання фрагменту коду? a,b,c=4,0,2 def g(b): global c    a-=2 b=3 c=1 return a+b+c a,b,c=5,1,6 print(g(b),a,b,c)

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
e = ('a','b','c','d','e',0,1,2)
print(e[-8])
```

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) {a=5;}
- ( 2 ) char c='\n';
- ( 3 ) int f(int a,9);
- ( 4 ) if a<b a=0;

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (!6==2 or !4< false or 6.0>2.0);
```

**5.** Що буде виведено за виконання фрагменту коду?

```cpp

a,b,c=7,6,8
def g(a,b,c=9):
    print(a,b,c,end="")

g(a=4,0,b=2)
print(a,b,c)

```

**6.** Що буде виведено за виконання фрагменту коду? x,y,z=4,0,2 y,x,z,z=y,7,z,x print(x,y,z)

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) x=y=z
- ( 2 ) ++n
- ( 3 ) True=1
- ( 4 ) n=1; n++

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) fop
- ( 3 ) <>
- ( 4 ) '123'

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print(4**-0.5*True)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int g(int b){
    int y = 50;
    if (b) 
        y = 4;
    else if (b == 0)
         return 0;
    else
         y = 2;
    return y;
}

int main(){
    cout << g(-4);
    return 0;
}
```

## Варіант 3

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << 9 / 9 / 9 / 9;
```

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) 0.5
- ( 2 ) or
- ( 3 ) -3J
- ( 4 ) "abc"""

**3.** Що буде виведено за виконання фрагменту коду? try: res = 4**-0.5*True if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**4.** Що буде виведено за виконання фрагменту коду? try: res = 8//8/8*8 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**5.** Вкажіть значення та тип виразу: "6.0j"==6

**6.** Відмітити все, що є коректним оператором С++:

- ( 1 ) x=y=z
- ( 2 ) ++n
- ( 3 ) True=1
- ( 4 ) n=1; n++

**7.** Що буде виведено за виконання фрагменту коду? try: for e in range(7, 7, -2): if e<=7: continue print(e, end=';');e = -5 if e<=7: break else: print(e,end=';') print(e) except: print('Z')

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int &x, int &y){
    x = 6;
    y-= 3;
    return y;
}

int main(){
    int a = 1, b = 4;
    b = f(b, b);
    cout << a << ":" << b <<':';
    {
        int a = 7, b = 2;
        cout << ((b<=4) || ((a-=2) <= 4))
             << a << ":" << b << ":";
    }
    {
        inta = 8;
        cout << a << ':';
    }
    cout << a << ":" << b << endl;
    return 0;
}
```

**9.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) fop
- ( 3 ) <>
- ( 4 ) '123'

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
m=4
while m<=10:
    m+=2
print(m)
```

## Варіант 4

**1.** Що буде виведено за виконання фрагменту коду? a = 4 b = 0 c = 2 def g(b): global c    a -= 2 b = 3 c = 1 return a + b + c a = 5 b = 1 c = 6 try: print(g(b), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')

**2.** Записати ВИРАЗ, значенням якого є множина, що складається з цілих чисел від 15 до 2005 (включно), що діляться на 3.

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) x=y=z
- ( 2 ) ++n
- ( 3 ) True=1
- ( 4 ) n=1; n++

**4.** Записати ВИРАЗ, значенням якого є список, що складається з квадратних коренів цілих чисел від 15 до 2005 (включно), що діляться на 3, записаних в оберненому порядку.

**5.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) fop
- ( 3 ) <>
- ( 4 ) '123'

**6.** Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення. Оцінюється правильність та якість коду.

**7.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) 0.5
- ( 2 ) or
- ( 3 ) -3J
- ( 4 ) "abc"""

**8.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') for e in range(7,7,-2): if e<=7: continue print(e, end=' ') e=-5 if e<=7: break else: print(e, end=' ') print(e)

**9.** Що буде виведено за виконання фрагменту коду? e=['0','1','2','3','4']; e.extend([1,2]); print(e)

**10.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в оберненому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
