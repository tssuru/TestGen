# Контрольна робота

## Варіант 1

**1.** Відмітити все, що є коректним оператором С++:

- ( 1 ) bool f(int a);
- ( 2 ) int f(int a,b);
- ( 3 ) cout<<3.4;
- ( 4 ) int n; bool n;

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
9**0.5*-2
```

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
print(8 > 4 <= 8.0 is True)
```

**4.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

**5.** Що буде виведено за виконання фрагменту коду? a,b,c=6,2,7 def h(a): global c    a=3 b*=2 c=4 return a+b+c a,b,c=9,4,3 print(h(a),a,b,c)

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (12 % 12 % 12 % 12);
```

**7.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

**8.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 6, 8, 7
def h(a, b, c):
    print(a, b, c, end=" ")

h(b=0, c=2, 1)
print(a, b, c)
```

**10.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') g=[10,11,12,13,14] print(g.pop(1), end=' ') print(g)

## Варіант 2

**1.** Що буде виведено за виконання фрагменту коду? a,b,c=6,2,7 def h(a): global c    a=3 b*=2 c=4 return a+b+c a,b,c=9,4,3 print(h(a),a,b,c)

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
g = ('a','b','c','d','e',0,1,2,3)
print(g[2])
```

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) bool f(int a);
- ( 2 ) int f(int a,b);
- ( 3 ) cout<<3.4;
- ( 4 ) int n; bool n;

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (!8<4 and !8.0> true and 9<=4.0);
```

**5.** Що буде виведено за виконання фрагменту коду?

```cpp

a,b,c=6,8,7
def h(a,b,c):
    print(a,b,c,end="")

h(b=0,c=2,1)
print(a,b,c)

```

**6.** Що буде виведено за виконання фрагменту коду? x,y,z=0,2,1 x,z,y,z=6,z,y,x print(x,y,z)

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print(9**0.5*-2)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int h(int a){
    int u = 58;
    if (a < 2) 
        return 6;
    if (a > 5)
         u = 2;
    else
         return 7;
    return u;
}

int main(){
    cout << h(-2);
    return 0;
}
```

## Варіант 3

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << 12 % 12 % 12 % 12;
```

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

**3.** Що буде виведено за виконання фрагменту коду? try: res = 9**0.5*-2 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**4.** Що буде виведено за виконання фрагменту коду? try: res = 5/5*5*5 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**5.** Вкажіть значення та тип виразу: 8<True

**6.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**7.** Що буде виведено за виконання фрагменту коду? try: for a in range(6, 6, -3): if a<3: break print(a, end=';') if a<3: break else: print('end',end=';') print(a) except: print('Z')

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int &x, int &y){
    x = 8;
    y+= 2;
    return x;
}

int main(){
    int a = 9, b = 5;
    a = f(a, a);
    cout << a << ":" << b <<':';
    {
        int a = 1, b = 6;
        cout << ((a<5) && ((b+=1) < 5))
             << a << ":" << b << ":";
    }
    {
        inta = 4;
        cout << a << ':';
    }
    cout << a << ":" << b << endl;
    return 0;
}
```

**9.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
i=6
while i<12:
    i+=1
print(i)
```

## Варіант 4

**1.** Що буде виведено за виконання фрагменту коду? a = 6 b = 2 c = 7 def h(a): global c    a = 3 b *= 2 c = 4 return a + b + c a = 9 b = 4 c = 3 try: print(h(a), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')

**2.** Записати ВИРАЗ, значенням якого є множина, що складається з цілих чисел від 17 до 2020 (включно), що не діляться на 2.

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**4.** Записати ВИРАЗ, значенням якого є список, що складається з кубів цілих чисел від 17 до 2020 (включно), що не діляться на жодне з чисел 2, 3, записаних в оберненому порядку.

**5.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

**6.** Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення. Оцінюється правильність та якість коду.

**7.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

**8.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') for a in range(6,6,-3): if a<3: break print(a, end=' ') if a<3: break else: print('end', end=' ') print(a)

**9.** Що буде виведено за виконання фрагменту коду? g=[10,11,12,13,14]; print(g.pop(1),end=' '); print(g)

**10.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

## Варіант 5

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
print(9**0.5*-2)
```

**2.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

**3.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).

**4.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') i=6 while i<12: i+=1 print(i)

**5.** Записати ВИРАЗ, значенням якого є множина, що складається з кубів цілих чисел від 17 до 2020 (включно), що не діляться на жодне з чисел 2, 3.

**6.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**7.** Що буде виведено за виконання фрагменту коду? try: i = 6 while i<12: i += 1 print(i, end=';') except: print('ERR')

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int a = 6, b = 2, c = 7;

int h(){
    inta = 6;
    intb = 9;
    intc = 4;
    return a + b + c;
}

int main(){
    inta = 3;
    intb = 0;
    intc = 8;
    cout << h() << ':';
    cout << a << ':' << b << ':' << c;
    return 0;
}
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
a,b,c=6,2,7
def h(a):
global c    a=3
    b*=2
    c=4
    return a+b+c

a,b,c=9,4,3
print(h(a),a,b,c)
```

## Варіант 6

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
a,b,c=6,2,7
def h(a):
global c    a=3
    b*=2
    c=4
    return a+b+c

a,b,c=9,4,3
print(h(a),a,b,c)
```

**2.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
for a in range(10,10+4,-3):
    if a<3:
        continue
    print(a, end=' ')
    a=10
    if a<3:
        break
else:
    print(13, end=' ')
print(a, end=' ')
```

**4.** Що буде виведено за виконання фрагменту коду?

```cpp
print(8>4>8.0>True)
```

**5.** Що буде виведено за виконання фрагменту коду?

```cpp
a = 6
b = 2
c = 7

def h(a):
global c    a=3
    b=2
    c=4
    return a+b+c

a, b, c = 9,4,3
try:
    print(h(a), a, b, c, sep=';')
except:
    print('Z', a, b, c, sep=';')
```

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
i=12
while i>6:
    i+=-3
print(i)
```

**7.** Що буде виведено за виконання фрагменту коду?

```cpp
def h(a):
    u=58
    if a<2: 
        return 6
    if a>5:
         u=2
    else:
         return 7
    return u

print(h(-2))
```

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
not8<4 and not8.0>True and 9<=4.0
```

**9.** Відмітити все, що є коректним оператором С++:

- ( 1 ) bool f(int a);
- ( 2 ) int f(int a,b);
- ( 3 ) cout<<3.4;
- ( 4 ) int n; bool n;

**10.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

## Варіант 7

**1.** Що буде виведено за виконання фрагменту коду? def h(a): u=58 if a<2: return 6 if a>5: u=2 else: return 7 return u print(h(-2))

**2.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**3.** Що буде виведено за виконання фрагменту коду?

```cpp
a,b,c=6,2,7
def h(a):
    a=3
    b=2
    c=4
    return a+b+c

a,b,c=9,4,3
print(h(a),a,b,c)
```

**4.** Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення.

**5.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
i=6
while i<12:
    i+=1
print(i)
```

**7.** Що буде виведено за виконання фрагменту коду?

```cpp
def h(a,b):
    c=58
    if a<2:
        return 6
    if a>5:
         c=2
    else: 
        return 7
    return c

print(h(-2,-2))
```

**8.** Відмітити все, що є коректним оператором С++:

- ( 1 ) bool f(int a);
- ( 2 ) int f(int a,b);
- ( 3 ) cout<<3.4;
- ( 4 ) int n; bool n;

**9.** Що буде виведено за виконання фрагменту коду? def f(n): print("f", end=""); return n print(f(-2) and f(1))

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
cout << (10 % 10 % 10 % 10);
```

## Варіант 8

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
a, b, c = 6, 8, 7
def h(a, b, c):
    print(a, b, c, end=" ")

h(b=0, c=2, 1)
print(a, b, c)
```

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
i=6
while i<12:
    i+=1
print(i)
```

**3.** Що буде виведено за виконання фрагменту коду? def f(n): print("f", end=""); return n print(f(-2) and f(1))

**4.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

**5.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int &x, int &y){
    x = 8;
    y+= 2;
    return x;
}

int main(){
    int a = 9, b = 5;
    a = f(a, a);
    cout << a << ":" << b <<':';
    {
        int a = 1, b = 6;
        cout << ((a<5) && ((b+=1) < 5)) << ':';
        cout << a << ":" << b << ':';
    }
    {
        inta = 4;
        cout << a << ':';
    }
    cout << a << ":" << b << endl;
    return 0;
}
```

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
print(5/5*5*5)
```

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
g=[10,11,12,13,14]
del g[6:]
print(g)
```

**9.** Вкажіть значення та тип виразу: not8<4 and not8.0>True and 9<=4.0

**10.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

## Варіант 9

**1.** Що буде виведено за виконання фрагменту коду?

```cpp
if (15 < 15)
    cout << "x";
else
    cout << "c";
```

**2.** Що буде виведено за виконання фрагменту коду?

```cpp
_a = 0

class A:
    _a = 1
    
    def __init__(self):
        self._a = 2
        _a = 3
        A._a = 4

obj = A()
try:
    print(_a, end=' ')
    print(obj._a, end=' ')
    print(A._a, end=' ')
    print(obj._a, end=' ')
    print(obj._A__a, end=' ')
except:
    print('ERR')
```

**3.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**4.** Відмітити все, що є коректним оператором С++:

- ( 1 ) bool f(int a);
- ( 2 ) int f(int a,b);
- ( 3 ) cout<<3.4;
- ( 4 ) int n; bool n;

**5.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
g=[10,11,12,13,14]
del g[6:]
print(g)
```

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
def h(a,b):
    c=58
    if a<2:
        return 6
    if a>5:
         c=2
    else: 
        return 7
    return c

print(h(-2,-2))
```

**7.** Що буде виведено за виконання фрагменту коду?

```cpp
i=6
while i<12:
    i+=1
print(i)
```

**8.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) False
- ( 2 ) '\'
- ( 3 ) py
- ( 4 ) 8,3

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
i=12
while i>6:
    i+=-3
print(i)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
i=6
while i<12:
    i+=1
print(i)
```

## Варіант 10

**1.** Що буде виведено за виконання фрагменту коду? g=[10,11,12,13,14]; del g[6:]; print(g)

**2.** Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Обхід дерева починається з кореня (на рис. це верхня вершина).

**3.** Відмітити все, що є однією правильною лексемою:

- ( 1 ) Switch
- ( 2 ) 0,5
- ( 3 ) 8.3
- ( 4 ) 8,3

**4.** Що буде виведено за виконання фрагменту коду? try: print(6, end=';') print(2j<6j, end=';') print(7, end=';') except TypeError: print(9, end=';') except ValueError: print(4, end=';') except: print('Z', end=';') else: print(3, end=';') finally: print(0)

**5.** Відмітити все, що є коректним оператором С++:

- ( 1 ) bool f(int a);
- ( 2 ) int f(int a,b);
- ( 3 ) cout<<3.4;
- ( 4 ) int n; bool n;

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
print(5/5*5*5)
```

**7.** Відмітити все, що є коректним оператором С++:

- ( 1 ) 3.14%2
- ( 2 ) if a<b: a=0 else: b=0
- ( 3 ) i,j=j,j
- ( 4 ) x+3=5

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>

int h(int a, int b){
    int c = 58;
    if (a < 2)
        return 6;
    if (a > 5)
         c = 2;
    else 
        return 7;
    return c;
}

int main(){
    std::cout << h(-2, -2);
    return 0;
}
```

**9.** Що буде виведено за виконання фрагменту коду? try: res = 8<True if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')

**10.** Що буде виведено за виконання фрагменту коду? def f(n): print('f', end='') return n try: print(f(-2) and f(1)) except: print('ERR')
