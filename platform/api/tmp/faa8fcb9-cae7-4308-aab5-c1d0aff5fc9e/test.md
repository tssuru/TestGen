# open-35

## Варіант 1

**1.** Що буде виведено за виконання фрагменту коду? k=2 while k<8: k+=1 print(k)

**2.** Що буде виведено за виконання фрагменту коду? a,b,c=2,9,0 def f(a): global c     a=4 b+=3 c=5 return a+b+c a,b,c=5,4,6 print(f(a),a,b,c)

**3.** Що буде виведено за виконання фрагменту коду? -2**-2+1

**4.** Що буде виведено за виконання фрагменту коду? Записати ВИРАЗ, значенням якого є список, що складається з квадратівцілих чисел від 13 до 2003 (включно), що не діляться на 5, записаних в оберненому порядку.

**5.** Що буде виведено за виконання фрагменту коду? a = 2 b = 9 c = 0 def f(a): global c    a = 4 b += 3 c = 5 return a + b + c a = 5 b = 4 c = 6 try: print(f(a), a, b, c, sep=';') except: print('ERR', a, b, c, sep=';')

**6.** Що буде виведено за виконання фрагменту коду?

```cpp
6/6//6*6
```

**7.** Що буде виведено за виконання фрагменту коду? True>"4"

**8.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

bool f(int n){
    cout<<"f";
    return n;
}

int main(){
    cout<<(f(-6) && f(0));
    return 0;
}
```

**9.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
c=[10,11,12,13,14]
c[-7:-7]='11'
print(c)
```

**10.** Що буде виведено за виконання фрагменту коду?

```cpp
def f(a,b):
    c=72
    if a>-2:
        return 2
    if b>=4:
         c=9
    else: 
        c=0
    return c

print(f(-6,-6))
```

**11.** Що буде виведено за виконання фрагменту коду? k=8 while k>2: k+=-3 print(k)

**12.** Що буде виведено за виконання фрагменту коду?

```cpp
a,b,c=2,9,0
def f(a):
global c    a=4
    b+=3
    c=5
    return a+b+c

a,b,c=5,4,6
print(f(a),a,b,c)
```

**13.** Що буде виведено за виконання фрагменту коду? print('R', end=' ') for c in range(-12,-12,2): if c>5: continue print(c, end=' ') if c>5: break else: print(c, end=' ') print(c)

**14.** Що буде виведено за виконання фрагменту коду? try: for c in range(-12, -12, 2): if c>5: continue print(c, end=';') if c>5: break else: print(c,end=';') print(c) except: print('ERR')

**15.** Що буде виведено за виконання фрагменту коду? c=[10,11,12,13,14]; c[-7:-7]='11'; print(c)

**16.** Що буде виведено за виконання фрагменту коду?

```cpp
print(4 >= 4.0 > 5 == True)
```

**17.** Що буде виведено за виконання фрагменту коду?

```cpp
print('R', end=' ')
k=2
while k<8:
    k+=1
print(k)
```

**18.** Що буде виведено за виконання фрагменту коду? c=[10,11,12,13,14]; c[-7:-7]='11'; print(c)

**19.** Що буде виведено за виконання фрагменту коду?

```cpp
a,b,c=2,9,0
def f(a):
    a=4
    b=3
    c=5
    return a+b+c

a,b,c=5,4,6
print(f(a),a,b,c)
```

**20.** Що буде виведено за виконання фрагменту коду? Записати ВИРАЗ, значенням якого є список, що складається з квадратівцілих чисел від 13 до 2003 (включно), що не діляться на 5, записаних в обернено\-му порядку.

**21.** Що буде виведено за виконання фрагменту коду? try: k = 8 while k>2: k += -3 print(k, end=';') except: print('ERR')

**22.** Що буде виведено за виконання фрагменту коду? try: k = 2 while k<8: k += 1 print(k, end=';') except: print('ERR')

**23.** Що буде виведено за виконання фрагменту коду?

```cpp
True>"4"
```

**24.** Що буде виведено за виконання фрагменту коду?

```cpp
#include <iostream>
using namespace std;

int f(int a, int b){
    int c = 72;
    if (a > -2)
        return 2;
    if (b >= 4)
         c = 9;
    else 
        c = 0;
    return c;
}

int main(){
    cout << f(-6, -6);
    return 0;
}
```

**25.** Що буде виведено за виконання фрагменту коду? try: for c in range(-12, -12, 2): if c>5: continue print(c, end=';') if c>5: break else: print(c,end=';') print(c) except: print('Z')

**26.** Що буде виведено за виконання фрагменту коду?

```cpp

x,y,z=2,3,0
z,x,x,z=x,8,z,y
print(x,y,z)
```

**27.** Що буде виведено за виконання фрагменту коду?

```cpp
a,b,c=2,9,0
def f(a):
global c    a=4
    b=3
    c=5
    return a+b+c

a,b,c=5,4,6
print(f(a),a,b,c)
```

**28.** Що буде виведено за виконання фрагменту коду?

```cpp

a,b,c=8,9,6
def f(a,b=7,c):
    print(a,b,c,end="")

f(2,a=3)
print(a,b,c)

```

**29.** Що буде виведено за виконання фрагменту коду? Записати ВИРАЗ, значенням якого є список, що складається з цілих чисел від 13 до 2003 (включно), причому спочатку за зростанням записано всі, що не діляться на 5, а потім решту за зростанням.

**30.** Що буде виведено за виконання фрагменту коду?

```cpp
for c in range(-12,-12,2):
    if c>5:
        continue
        print(c,end=' ')
    if c>5:
        break
else:
    print(c, end=' ')
print(c, end=' ')
```

**31.** Що буде виведено за виконання фрагменту коду?

```cpp
True>"4"
```

**32.** Що буде виведено за виконання фрагменту коду? for c in range(-12,-12,2): if c>5: continue print(c,end=' ') if c>5: break else: print(c,end=' ') print(c)

**33.** Що буде виведено за виконання фрагменту коду?

```cpp

try:
    print(2,end="")
    print(int("9"),end="")
    print(0,end="")
except ValueError: print(5,end="")
except TypeError: print(4,end="")
else: print(6,end="")
finally: print(7,end="")

```

**34.** Що буде виведено за виконання фрагменту коду? Записати ВИРАЗ, значенням якого є словник, ключами якого є цілі числа від 13 до 2003 (включно), що не діляться на 5, а ключам відповідають їх квадрати

**35.** Що буде виведено за виконання фрагменту коду? Записати ВИРАЗ, значенням якого є список, що складається з цілих чисел від 13 до 2003 (включно), причому спочатку за зростанням записано всі, що не діляться на 5, а потім решту за зростанням.
