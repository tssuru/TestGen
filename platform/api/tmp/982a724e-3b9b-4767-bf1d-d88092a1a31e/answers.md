# Ключ відповідей

**Контрольна робота**
Створено: 2026-03-05T16:05:57.439Z

## Варіант 1

1. Відмітити все, що є коректним оператором С++:
   Правильні: (2) bool f(int a);, (4) j=i;

2. Що буде виведено за виконання фрагменту коду? 4**2.0+2
   Тип: відкрита відповідь

3. Що буде виведено за виконання фрагменту коду? print(5.0 > 3.0 <= True != 5)
   Тип: відкрита відповідь

4. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

5. Що буде виведено за виконання фрагменту коду? a,b,c=3,1,5 def h(a): global c    a=3 b*=2 c=4 return a+b+c a,b,c=0,9,6 print(h(b),a,b,c)
   Тип: відкрита відповідь

6. Що буде виведено за виконання фрагменту коду? cout << (5 % 5 % 5 % 5);
   Тип: відкрита відповідь

7. Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
   Тип: відкрита відповідь

8. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

9. Що буде виведено за виконання фрагменту коду? a, b, c = 9, 7, 8 def h(a, b=6, c=7): print(a, b, c, end=" ") h(3, 1, c=5) print(a, b, c)
   Тип: відкрита відповідь

10. Що буде виведено за виконання фрагменту коду? print('R', end=' ') d=[10,11,12,13,14] print(d.pop(-2), end=' ') print(d)
   Тип: відкрита відповідь

## Варіант 2

1. Що буде виведено за виконання фрагменту коду? a,b,c=3,1,5 def h(a): global c    a=3 b*=2 c=4 return a+b+c a,b,c=0,9,6 print(h(b),a,b,c)
   Тип: відкрита відповідь

2. Що буде виведено за виконання фрагменту коду? print('R', end=' ') d = ('a','b','c','d','e',0,1,2,3) print(d[-11])
   Тип: відкрита відповідь

3. Відмітити все, що є коректним оператором С++:
   Правильні: (2) bool f(int a);, (4) j=i;

4. Що буде виведено за виконання фрагменту коду? cout << (!5.0>=3.0 || !true<= 5 || 3!=7);
   Тип: відкрита відповідь

5. Що буде виведено за виконання фрагменту коду? a,b,c=9,7,8 def h(a,b=6,c=7): print(a,b,c,end="") h(3,1,c=5) print(a,b,c)
   Тип: відкрита відповідь

6. Що буде виведено за виконання фрагменту коду? x,y,z=3,1,5 x,y,z,x=9,z,y,7 print(x,y,z)
   Тип: відкрита відповідь

7. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

8. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

9. Що буде виведено за виконання фрагменту коду? print(4**2.0+2)
   Тип: відкрита відповідь

10. Що буде виведено за виконання фрагменту коду? #include <iostream> using namespace std; int h(int d) int x = 69; if (d >= -3) return 3; if (d <= -4) x = 1; else return 5; return x; int main() cout << h(9); return 0;
   Тип: відкрита відповідь

## Варіант 3

1. Що буде виведено за виконання фрагменту коду? cout << 5 % 5 % 5 % 5;
   Тип: відкрита відповідь

2. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

3. Що буде виведено за виконання фрагменту коду? try: res = 4**2.0+2 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')
   Тип: відкрита відповідь

4. Що буде виведено за виконання фрагменту коду? try: res = 5/5%5*5 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')
   Тип: відкрита відповідь

5. Вкажіть значення та тип виразу: "3">=3.0
   Тип: відкрита відповідь

6. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

7. Що буде виведено за виконання фрагменту коду? try: for d in range(3, 3, -3): if d>=6: break print(d, end=';') if d>=6: break else: print(d,end=';') print(d) except: print('Z')
   Тип: відкрита відповідь

8. Що буде виведено за виконання фрагменту коду? #include <iostream> using namespace std; int f(int &x, int &y) x = 3; y+= 2; return x; int main() int a = 6, b = 5; a = f(a, a); cout << a << ":" << b <<':'; int a = 1, b = 4; cout << ((a>=5) && ((b+=1) >= 5)) << a << ":" << b << ":"; inta = 8; cout << a << ':'; cout << a << ":" << b << endl; return 0;
   Тип: відкрита відповідь

9. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

10. Що буде виведено за виконання фрагменту коду? print('R', end=' ') n=3 while n<7: n+=1 print(n)
   Тип: відкрита відповідь

## Варіант 4

1. Що буде виведено за виконання фрагменту коду? a = 3 b = 1 c = 5 def h(a): global c    a = 3 b *= 2 c = 4 return a + b + c a = 0 b = 9 c = 6 try: print(h(b), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')
   Тип: відкрита відповідь

2. Записати ВИРАЗ, значенням якого є множина, що складається з цілих чисел від 12 до 2018 (включно), що не діляться на 7.
   Тип: відкрита відповідь

3. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

4. Записати ВИРАЗ, значенням якого є список, що складається з кубів цілих чисел від 12 до 2018 (включно), що не діляться на жодне з чисел 2, 3, записаних в оберненому порядку.
   Тип: відкрита відповідь

5. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

6. Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення. Оцінюється правильність та якість коду.
   Тип: відкрита відповідь

7. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

8. Що буде виведено за виконання фрагменту коду? print('R', end=' ') for d in range(3,3,-3): if d>=6: break print(d, end=' ') if d>=6: break else: print(d, end=' ') print(d)
   Тип: відкрита відповідь

9. Що буде виведено за виконання фрагменту коду? d=[10,11,12,13,14]; print(d.pop(-2),end=' '); print(d)
   Тип: відкрита відповідь

10. Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
   Тип: відкрита відповідь

## Варіант 5

1. Що буде виведено за виконання фрагменту коду? print(4**2.0+2)
   Тип: відкрита відповідь

2. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

3. Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Алгоритм починає роботу з кореня (на рис. це верхня вершина).
   Тип: відкрита відповідь

4. Що буде виведено за виконання фрагменту коду? print('R', end=' ') n=3 while n<7: n+=1 print(n)
   Тип: відкрита відповідь

5. Записати ВИРАЗ, значенням якого є множина, що складається з кубів цілих чисел від 12 до 2018 (включно), що не діляться на жодне з чисел 2, 3.
   Тип: відкрита відповідь

6. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

7. Що буде виведено за виконання фрагменту коду? try: n = 3 while n<7: n += 1 print(n, end=';') except: print('ERR')
   Тип: відкрита відповідь

8. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

9. Що буде виведено за виконання фрагменту коду? #include <iostream> using namespace std; int a = 3, b = 1, c = 5; int h() inta = 3; intb = 0; intc = 9; return a + b + c; int main() inta = 6; intb = 4; intc = 8; cout << h() << ':'; cout << a << ':' << b << ':' << c; return 0;
   Тип: відкрита відповідь

10. Що буде виведено за виконання фрагменту коду? a,b,c=3,1,5 def h(a): global c    a=3 b*=2 c=4 return a+b+c a,b,c=0,9,6 print(h(b),a,b,c)
   Тип: відкрита відповідь

## Варіант 6

1. Що буде виведено за виконання фрагменту коду? a,b,c=3,1,5 def h(a): global c    a=3 b*=2 c=4 return a+b+c a,b,c=0,9,6 print(h(b),a,b,c)
   Тип: відкрита відповідь

2. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

3. Що буде виведено за виконання фрагменту коду? for d in range(8,8+4,3): if d>=6: pass print(d, end=' ') d=8 if d>=6: break else: print(d, end=' ') print(d, end=' ')
   Тип: відкрита відповідь

4. Що буде виведено за виконання фрагменту коду? print(5.0>3.0>True>5)
   Тип: відкрита відповідь

5. Що буде виведено за виконання фрагменту коду? a = 3 b = 1 c = 5 def h(a): global c    a=3 b=2 c=4 return a+b+c a, b, c = 0,9,6 try: print(h(b), a, b, c, sep=';') except: print('Z', a, b, c, sep=';')
   Тип: відкрита відповідь

6. Що буде виведено за виконання фрагменту коду? print('R', end=' ') n=7 while n>3: n+=-3 print(n)
   Тип: відкрита відповідь

7. Що буде виведено за виконання фрагменту коду? def h(d): x=69 if d>=-3: return 3 if d<=-4: x=1 else: return 5 return x print(h(9))
   Тип: відкрита відповідь

8. Що буде виведено за виконання фрагменту коду? not5.0>=3.0 and notTrue<=5 and 3!=7
   Тип: відкрита відповідь

9. Відмітити все, що є коректним оператором С++:
   Правильні: (2) bool f(int a);, (4) j=i;

10. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

## Варіант 7

1. Що буде виведено за виконання фрагменту коду? def h(d): x=69 if d>=-3: return 3 if d<=-4: x=1 else: return 5 return x print(h(9))
   Тип: відкрита відповідь

2. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

3. Що буде виведено за виконання фрагменту коду? a,b,c=3,1,5 def h(a): a=3 b=2 c=4 return a+b+c a,b,c=0,9,6 print(h(b),a,b,c)
   Тип: відкрита відповідь

4. Написати програму, що запитує у користувача значення дійсних параметрів x та y, обчислює для них значення виразу та повiдомляє введенi данi та результат обчислення.
   Тип: відкрита відповідь

5. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

6. Що буде виведено за виконання фрагменту коду? n=3 while n<7: n+=1 print(n)
   Тип: відкрита відповідь

7. Що буде виведено за виконання фрагменту коду? def h(a,b): c=69 if b>=-3: return 3 if a<=-4: c=1 else: return 5 return c print(h(9,9))
   Тип: відкрита відповідь

8. Відмітити все, що є коректним оператором С++:
   Правильні: (2) bool f(int a);, (4) j=i;

9. Що буде виведено за виконання фрагменту коду? def f(n): print("f", end=""); return n print(f(9) and f(-8))
   Тип: відкрита відповідь

10. Що буде виведено за виконання фрагменту коду? cout << (5 % 5 % 5 % 5);
   Тип: відкрита відповідь

## Варіант 8

1. Що буде виведено за виконання фрагменту коду? a, b, c = 9, 7, 8 def h(a, b=6, c=7): print(a, b, c, end=" ") h(3, 1, c=5) print(a, b, c)
   Тип: відкрита відповідь

2. Що буде виведено за виконання фрагменту коду? n=3 while n<7: n+=1 print(n)
   Тип: відкрита відповідь

3. Що буде виведено за виконання фрагменту коду? def f(n): print("f", end=""); return n print(f(9) and f(-8))
   Тип: відкрита відповідь

4. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

5. Що буде виведено за виконання фрагменту коду? #include <iostream> using namespace std; int f(int &x, int &y) x = 3; y+= 2; return x; int main() int a = 6, b = 5; a = f(a, a); cout << a << ":" << b <<':'; int a = 1, b = 4; cout << ((a>=5) && ((b+=1) >= 5)) << ':'; cout << a << ":" << b << ':'; inta = 8; cout << a << ':'; cout << a << ":" << b << endl; return 0;
   Тип: відкрита відповідь

6. Що буде виведено за виконання фрагменту коду? print(5/5%5*5)
   Тип: відкрита відповідь

7. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

8. Що буде виведено за виконання фрагменту коду? print('R', end=' ') d=[10,11,12,13,14] del d[-6:-6] print(d)
   Тип: відкрита відповідь

9. Вкажіть значення та тип виразу: not5.0>=3.0 and notTrue<=5 and 3!=7
   Тип: відкрита відповідь

10. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

## Варіант 9

1. Що буде виведено за виконання фрагменту коду? if (3 >= 3) cout << "x"; else cout << "b";
   Тип: відкрита відповідь

2. Що буде виведено за виконання фрагменту коду? _d = 0 class D: _d = 1 def __init__(self): self._d = 2 _d = 3 D._d = 4 obj = D() try: print(_d, end=' ') print(obj._d, end=' ') print(D._d, end=' ') print(obj._d, end=' ') print(obj._D__d, end=' ') except: print('ERR')
   Тип: відкрита відповідь

3. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

4. Відмітити все, що є коректним оператором С++:
   Правильні: (2) bool f(int a);, (4) j=i;

5. Що буде виведено за виконання фрагменту коду? print('R', end=' ') d=[10,11,12,13,14] del d[-6:-6] print(d)
   Тип: відкрита відповідь

6. Що буде виведено за виконання фрагменту коду? def h(a,b): c=69 if b>=-3: return 3 if a<=-4: c=1 else: return 5 return c print(h(9,9))
   Тип: відкрита відповідь

7. Що буде виведено за виконання фрагменту коду? n=3 while n<7: n+=1 print(n)
   Тип: відкрита відповідь

8. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) 0.2E3, (4) x23

9. Що буде виведено за виконання фрагменту коду? print('R', end=' ') n=7 while n>3: n+=-3 print(n)
   Тип: відкрита відповідь

10. Що буде виведено за виконання фрагменту коду? print('R', end=' ') n=3 while n<7: n+=1 print(n)
   Тип: відкрита відповідь

## Варіант 10

1. Що буде виведено за виконання фрагменту коду? d=[10,11,12,13,14]; del d[-6:-6]; print(d)
   Тип: відкрита відповідь

2. Указати послідовність міток вершин дерева, зображеного на рис., що утвориться в результаті обходу дерева в прямому порядку. Мітки записувати через пробіл. Обхід дерева починається з кореня (на рис. це верхня вершина).
   Тип: відкрита відповідь

3. Відмітити все, що є однією правильною лексемою:
   Правильні: (2) LIFO, (4) x23

4. Що буде виведено за виконання фрагменту коду? try: print(3, end=';') print(1j>=3j, end=';') print(5, end=';') except TypeError: print(0, end=';') except ValueError: print(9, end=';') except: print('Z', end=';') else: print(6, end=';') finally: print(4)
   Тип: відкрита відповідь

5. Відмітити все, що є коректним оператором С++:
   Правильні: (2) bool f(int a);, (4) j=i;

6. Що буде виведено за виконання фрагменту коду? print(5/5%5*5)
   Тип: відкрита відповідь

7. Відмітити все, що є коректним оператором С++:
   Правильні: (2) x=3**1, (4) --n

8. Що буде виведено за виконання фрагменту коду? #include <iostream> int h(int a, int b) int c = 69; if (b >= -3) return 3; if (a <= -4) c = 1; else return 5; return c; int main() std::cout << h(9, 9); return 0;
   Тип: відкрита відповідь

9. Що буде виведено за виконання фрагменту коду? try: res = "3">=3.0 if isinstance(res, bool): print(f'bool;') elif isinstance(res, int): print(f'int;') elif isinstance(res, float): print(f'float;') else: print('???') except: print('Z')
   Тип: відкрита відповідь

10. Що буде виведено за виконання фрагменту коду? def f(n): print('f', end='') return n try: print(f(9) and f(-8)) except: print('ERR')
   Тип: відкрита відповідь
